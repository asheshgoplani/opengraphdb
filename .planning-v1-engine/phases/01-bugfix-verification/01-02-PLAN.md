---
phase: 01-bugfix-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ogdb-cli/src/lib.rs
autonomous: true
requirements:
  - BUG-10
  - BUG-11
  - BUG-12
  - BUG-13
  - BUG-14
  - BUG-15

must_haves:
  truths:
    - "CLI path-bearing commands accept --db <path> as fallback when positional path is omitted"
    - "query command treats query text as single argument so --format flags work correctly"
    - "CALL statements route through core query engine, not legacy handler"
    - "import returns actionable error when database file does not exist"
    - "serve startup output includes protocol + bind endpoint for bolt, http, and mcp"
    - "serve accepts --port with protocol-aware defaults (Bolt 7687, HTTP 8080, gRPC 7689, MCP 7687)"
  artifacts:
    - path: "crates/ogdb-cli/src/lib.rs"
      provides: "All 6 CLI bugfix regression tests"
      contains: "global_db_flag_can_supply_database_path"
  key_links:
    - from: "crates/ogdb-cli/src/lib.rs (tests)"
      to: "run(&[String])"
      via: "CLI run function invocation in each test"
      pattern: 'run\(&vec!\['
---

<objective>
Audit and verify all 6 CLI bugfix regression tests in ogdb-cli (BUG-10 through BUG-15).

Purpose: Research confirmed that regression tests exist for all 6 bugs, with one potential gap: BUG-14 bolt/HTTP startup output assertion coverage. This plan audits each test to confirm it exercises the specific bugfix behavior, and adds targeted tests for the BUG-14 gap if confirmed.

Output: All 6 ogdb-cli regression tests confirmed adequate; BUG-14 bolt/HTTP startup output gap assessed and closed if needed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bugfix-verification/01-RESEARCH.md

@crates/ogdb-cli/src/lib.rs (test module at bottom of file)

<interfaces>
Test helpers available in ogdb-cli test module:
```rust
fn temp_db_path(tag: &str) -> PathBuf;
fn temp_file_path(tag: &str, ext: &str) -> PathBuf;
fn wal_path(path: &PathBuf) -> PathBuf;
fn meta_path(path: &PathBuf) -> PathBuf;
fn run(args: &Vec<String>) -> CliResult;
```

CliResult structure:
```rust
struct CliResult {
    exit_code: i32,
    stdout: String,
    stderr: String,
}
```

Serve handler functions that emit startup output:
```rust
fn handle_serve_bolt(...) // emits "listening on bolt://..." via eprintln!
fn handle_serve_http(...) // emits "listening on http://..."
fn handle_serve_mcp(...) // emits "listening on mcp://..."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit regression tests for BUG-10 through BUG-13</name>
  <files>crates/ogdb-cli/src/lib.rs</files>
  <action>
For each of the 4 bugs below, locate the existing test function in the `#[cfg(test)] mod tests` block at the bottom of `crates/ogdb-cli/src/lib.rs`, read its body, and verify it covers the requirement. Only modify the file if a gap is found.

**BUG-10: CLI --db path fallback**
- Find: `global_db_flag_can_supply_database_path` (~line 6760)
- Also find: `all_path_subcommands_accept_db_without_positional_path` (~line 6779)
- Also find: `query_allows_db_flag_without_positional_path` (~line 7690)
- Verify: Tests exercise path-bearing commands (info, stats, schema, neighbors, etc.) with `--db <path>` instead of a positional path. The `all_path_subcommands_accept_db_without_positional_path` test should cover a broad set of subcommands.
- Requirement met when: Multiple subcommands succeed with `--db` flag and no positional path.

**BUG-11: query argument parsing (--format flags)**
- Find: `query_parses_format_flag_after_query_argument` (~line 7709)
- Verify: Test runs something like `query <path> info --format json` and asserts the `--format json` flag is parsed correctly (not consumed as query text).
- Requirement met when: Test asserts exit_code == 0 and output is valid JSON (or the expected format).

**BUG-12: CALL routing through core engine**
- Find: `query_command_routes_call_procedures_and_create_index_on` (~line 7631)
- Verify: Test runs `CALL db.indexes()` and/or `CALL db.algo.shortestPath(...)` through the CLI `run(...)` function, asserting successful exit code and valid output.
- Requirement met when: CALL statements produce exit_code 0 and non-empty stdout.

**BUG-13: import missing database error**
- Find: `import_reports_missing_database_with_actionable_message` (~line 8601)
- Verify: Test runs `import` on a non-existent database path and asserts exit_code == 1 with the exact error message including "Run 'ogdb init <path>' first."
- Requirement met when: Test asserts both exit code and exact error message format.

**If a gap is found:** Add minimal assertions or a new test following the existing pattern (temp_db_path, run(&vec![...]), assert exit_code/stdout/stderr, cleanup). If all tests are adequate, do not modify the file.
  </action>
  <verify>
    <automated>cd /Users/ashesh/opengraphdb && cargo test -p ogdb-cli -- global_db_flag_can_supply_database_path all_path_subcommands_accept_db_without_positional_path query_allows_db_flag_without_positional_path query_parses_format_flag_after_query_argument query_command_routes_call_procedures_and_create_index_on import_reports_missing_database_with_actionable_message 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 test functions for BUG-10 through BUG-13 pass. Each test exercises the specific bugfix behavior described in REQUIREMENTS.md. Any coverage gaps have been filled.</done>
</task>

<task type="auto">
  <name>Task 2: Audit regression tests for BUG-14 and BUG-15, close bolt/HTTP startup gap</name>
  <files>crates/ogdb-cli/src/lib.rs</files>
  <action>
Audit the existing tests for BUG-14 and BUG-15, then evaluate and close the identified gap for BUG-14 bolt/HTTP startup output.

**BUG-14: serve startup output includes protocol + endpoint**
- Find: `serve_processes_single_tcp_request_when_max_requests_is_set` (~line 12936)
  - Verify: Test starts an MCP server and asserts stdout contains "listening on mcp://".
- Find: `http_serve_reports_bind_errors_and_timeout_helper_panics` (~line 13903)
  - Verify: Test exercises `handle_serve_http` but may only test error paths.
- Research gap: The implementation emits "listening on bolt://..." from `handle_serve_bolt` (line 2881) and "listening on http://..." from `handle_serve_http`, but no test directly asserts these strings from a successful serve startup.

**Evaluate the gap:**
1. Read the `serve_processes_single_tcp_request_when_max_requests_is_set` test body carefully. Note its pattern: it spawns a thread, starts the serve, connects, sends a request, and reads the output.
2. Read the `http_serve_reports_bind_errors_and_timeout_helper_panics` test body. Determine whether it asserts "listening on http://" or only tests error conditions.
3. If neither existing test asserts "listening on bolt://" from a successful bolt serve startup, write a new test following the pattern from `serve_processes_single_tcp_request_when_max_requests_is_set`. The new test should:
   - Use `temp_db_path("serve-bolt-startup-output")` and init a db
   - Start bolt serve on port 0 (OS-assigned) with `--max-requests 1`
   - Connect a TCP client to the bound port (use `std::net::TcpStream::connect_timeout`)
   - Assert stdout or stderr contains "listening on bolt://"
   - Clean up temp files
   - Follow Pitfall 4 from research: use port 0 to avoid port collisions
4. If the HTTP startup output is also not asserted from a success path, consider whether the bolt test alone is sufficient (since the startup output pattern is identical across all protocols, just with different scheme prefixes). If you judge the bolt test alone covers the BUG-14 requirement adequately across protocols, document that reasoning. If not, add an HTTP startup test as well.

**BUG-15: serve --port with protocol-aware defaults**
- Find: `resolve_serve_bind_addr_defaults_and_port_override` (~line 12847)
- Also find: `serve_accepts_http_port_flag` (~line 12895)
- Verify: First test exercises `resolve_serve_bind_addr` with all four protocols and various port overrides. Second test exercises the CLI argument path for `--port` on HTTP.
- Requirement met when: Tests cover all protocol defaults (Bolt 7687, HTTP 8080, gRPC 7689, MCP 7687) and port override behavior.

**If adding a new test:** Follow the existing serve test patterns exactly. Use `thread::spawn` for the server, port 0 for binding, and `--max-requests 1` to ensure the server exits cleanly after the test. Keep the test self-contained with proper cleanup.
  </action>
  <verify>
    <automated>cd /Users/ashesh/opengraphdb && cargo test -p ogdb-cli -- serve_processes_single_tcp_request_when_max_requests_is_set http_serve_reports_bind_errors_and_timeout_helper_panics resolve_serve_bind_addr_defaults_and_port_override serve_accepts_http_port_flag 2>&1 | tail -20</automated>
  </verify>
  <done>All existing tests for BUG-14 and BUG-15 pass. The BUG-14 startup output gap has been evaluated: either existing coverage is documented as sufficient, or a targeted bolt startup output test has been added and passes. BUG-15 protocol-aware port default tests cover all four protocols.</done>
</task>

</tasks>

<verification>
Run all ogdb-cli tests to confirm no regressions:
```bash
cargo test -p ogdb-cli 2>&1 | tail -5
```
Expected: all tests pass (100+ tests, 0 failures).
</verification>

<success_criteria>
- All test functions covering BUG-10 through BUG-15 have been audited
- Each test exercises the specific behavior described in REQUIREMENTS.md
- The BUG-14 bolt/HTTP startup output gap has been evaluated and closed
- All tests pass when run with `cargo test -p ogdb-cli`
- Any gaps found have been filled with targeted additions following existing test patterns
</success_criteria>

<output>
After completion, create `.planning/phases/01-bugfix-verification/01-02-SUMMARY.md`
</output>
