---
phase: 01-foundation-and-graph-visualization
plan: 05
type: execute
wave: 3
depends_on: [03]
files_modified:
  - frontend/src/components/layout/Header.tsx
  - frontend/src/components/layout/ConnectionStatus.tsx
  - frontend/src/components/layout/SettingsDialog.tsx
  - frontend/src/components/layout/ThemeToggle.tsx
autonomous: true
requirements: [FOUND-03, FOUND-04, SCHEMA-02]

must_haves:
  truths:
    - "User can see a colored dot in the header indicating connection status (green/red/amber)"
    - "User can open a settings dialog to configure the backend server URL"
    - "User can toggle between light and dark mode using a button in the header"
    - "Dark mode applies across the entire app including the graph canvas"
    - "Layout is responsive on desktop and tablet viewports"
  artifacts:
    - path: "frontend/src/components/layout/ConnectionStatus.tsx"
      provides: "Health status indicator dot"
      exports: ["ConnectionStatus"]
    - path: "frontend/src/components/layout/SettingsDialog.tsx"
      provides: "Settings dialog for server URL configuration"
      exports: ["SettingsDialog"]
    - path: "frontend/src/components/layout/ThemeToggle.tsx"
      provides: "Light/dark mode toggle button"
      exports: ["ThemeToggle"]
  key_links:
    - from: "frontend/src/components/layout/ConnectionStatus.tsx"
      to: "frontend/src/api/queries.ts"
      via: "useHealthCheck hook"
      pattern: "useHealthCheck"
    - from: "frontend/src/components/layout/SettingsDialog.tsx"
      to: "frontend/src/stores/settings.ts"
      via: "reads/writes serverUrl"
      pattern: "useSettingsStore"
    - from: "frontend/src/components/layout/ThemeToggle.tsx"
      to: "frontend/src/stores/settings.ts"
      via: "reads/writes theme"
      pattern: "useSettingsStore.*theme"
---

<objective>
Build the header toolbar components: connection status indicator, settings dialog, and theme toggle. Ensure dark mode and responsive layout work across all surfaces.

Purpose: Complete the header toolbar with live connection status, configurable server URL, and theme switching. Verify dark mode covers all surfaces including the canvas.
Output: ConnectionStatus dot, SettingsDialog, ThemeToggle, and a fully responsive layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-graph-visualization/01-RESEARCH.md
@.planning/phases/01-foundation-and-graph-visualization/01-CONTEXT.md
@.planning/phases/01-foundation-and-graph-visualization/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-graph-visualization/01-02-SUMMARY.md
@.planning/phases/01-foundation-and-graph-visualization/01-03-SUMMARY.md

<interfaces>
From frontend/src/api/queries.ts:
```typescript
export function useHealthCheck(): UseQueryResult<HealthStatus>;
// Returns { data: { connected: boolean }, isLoading, isError, isFetching }
```

From frontend/src/stores/settings.ts:
```typescript
export const useSettingsStore: {
  serverUrl: string; theme: 'light' | 'dark' | 'system'; resultLimit: number;
  setServerUrl: (url: string) => void; setTheme: (theme: 'light' | 'dark' | 'system') => void;
  setResultLimit: (limit: number) => void;
}
```

From frontend/src/components/ui/dialog.tsx:
shadcn Dialog component (Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter)

From frontend/src/components/ui/button.tsx:
shadcn Button component with variant prop ('default', 'ghost', 'outline', 'destructive')

From frontend/src/components/ui/input.tsx:
shadcn Input component
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create connection status, theme toggle, and settings dialog</name>
  <files>
    frontend/src/components/layout/ConnectionStatus.tsx,
    frontend/src/components/layout/ThemeToggle.tsx,
    frontend/src/components/layout/SettingsDialog.tsx
  </files>
  <action>
    1. Create frontend/src/components/layout/ConnectionStatus.tsx:
       - Use useHealthCheck() hook
       - Display a small colored dot (w-2.5 h-2.5 rounded-full) in the header
       - Colors: green (bg-green-500) when connected, red (bg-red-500) when disconnected, amber (bg-amber-500) when loading/fetching
       - Derive state: isLoading || isFetching -> amber, data?.connected -> green, else red
       - Add pulse animation on the amber state (animate-pulse)
       - Tooltip or text next to dot: "Connected" / "Disconnected" / "Connecting..."
       - Use text-xs text-muted-foreground for status text

    2. Create frontend/src/components/layout/ThemeToggle.tsx:
       - Icon button (shadcn Button variant="ghost" size="icon")
       - Show Sun icon in dark mode, Moon icon in light mode
       - On click: cycle through system -> light -> dark -> system
       - Use useSettingsStore to read and write theme
       - Import Sun, Moon, Monitor from lucide-react
       - Show Monitor icon when theme is 'system'

    3. Create frontend/src/components/layout/SettingsDialog.tsx:
       - shadcn Dialog triggered by a gear icon button (Settings icon from lucide-react)
       - Dialog content:
         a. Title: "Settings"
         b. Server URL input: shadcn Input, pre-filled with current serverUrl from settings store
         c. Result Limit input: shadcn Input type="number", pre-filled with resultLimit
         d. Save button: updates settings store with new values, closes dialog
         e. Cancel button: closes without saving
       - Use local state for form values, only commit to store on Save
       - Show current connection status inside the dialog for feedback
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 && echo "TYPES OK"</automated>
  </verify>
  <done>
    ConnectionStatus shows colored dot reflecting health check status. ThemeToggle cycles through system/light/dark modes. SettingsDialog allows configuring server URL and result limit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire header components and ensure responsive layout</name>
  <files>
    frontend/src/components/layout/Header.tsx,
    frontend/src/App.tsx
  </files>
  <action>
    1. Update frontend/src/components/layout/Header.tsx:
       - Replace placeholder slots with actual components:
         a. Left side: "OpenGraphDB" text (font-semibold text-lg)
         b. Right side: flex gap-2 items-center containing ConnectionStatus, ThemeToggle, SettingsDialog trigger
       - Responsive: on small screens (below md), reduce padding and font size
       - Header height: h-12, border-b, bg-card

    2. Verify responsive layout in App.tsx:
       - Ensure the overall layout uses flex column with h-screen
       - QueryInput area: on small screens, textarea may stack above button (flex-col instead of flex-row below sm breakpoint)
       - Graph canvas: fills remaining space, no fixed height (flex-1)
       - Property panel: Sheet overlay, does not push content (overlays at 320px width)
       - Test at common viewports: 1440px desktop, 1024px tablet landscape, 768px tablet portrait
       - Ensure no horizontal overflow at 768px

    3. Verify dark mode across all surfaces:
       - HTML document gets 'dark' class from ThemeProvider
       - Tailwind dark: variants apply to all shadcn components
       - Graph canvas backgroundColor updates from useGraphColors hook
       - Node labels and edge colors adapt to theme
       - All text is readable in both themes (no light-on-light or dark-on-dark)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 && npm run build 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <done>
    Header shows ConnectionStatus dot, ThemeToggle button, and Settings gear button. Layout responsive on desktop and tablet. Dark mode works across all surfaces including graph canvas. No horizontal overflow at 768px.
  </done>
</task>

</tasks>

<verification>
- Connection status dot: green when backend running, red when not, amber during initial load
- Settings dialog: can change server URL, new URL takes effect for health polling and queries
- Theme toggle: clicking cycles system -> light -> dark, graph canvas background changes accordingly
- Responsive: layout works at 1440px, 1024px, and 768px without horizontal overflow
- Dark mode: all surfaces (header, query input, graph canvas, property panel, table view) adapt
- Build completes under 500KB initial bundle
</verification>

<success_criteria>
- Live connection status indicator updates every 5 seconds
- Server URL configurable through settings dialog
- Theme toggle works across all surfaces including canvas
- Layout is responsive on desktop and tablet viewports
- No dark mode regressions (all text readable, all backgrounds correct)
- Build succeeds, bundle under 500KB
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-graph-visualization/01-05-SUMMARY.md`
</output>
