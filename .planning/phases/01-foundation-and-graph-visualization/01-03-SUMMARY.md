## Phase 01-03 Summary

- Query input executes Cypher with `Ctrl/Cmd+Enter` and auto-appends `LIMIT` when absent.
- App mutation flow is now centralized so query execution updates the rendered results.
- Force graph canvas renders labeled nodes, directional edges, and persistent edge-type labels.
