## Phase 01-02 Summary

- API client (`ApiClient`) and TanStack Query hooks (`useHealthCheck`, `useCypherQuery`) are in place.
- Zustand stores for settings, query state, and graph selection are implemented.
- Theme provider is wired in `main.tsx` and now resolves system/light/dark modes consistently.
