---
phase: 01-foundation-and-graph-visualization
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - frontend/src/components/layout/PropertyPanel.tsx
  - frontend/src/components/results/TableView.tsx
  - frontend/src/components/results/ResultsView.tsx
  - frontend/src/components/results/ResultsBanner.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [GRAPH-03, GRAPH-04, GRAPH-07, GRAPH-08]

must_haves:
  truths:
    - "User can click a node to see its properties in a right-side panel"
    - "User can click an edge to see its properties in a right-side panel"
    - "User can toggle between graph view and table view without re-executing the query"
    - "Result set shows truncation banner when limit fires"
    - "Clicking canvas background or pressing Escape closes the property panel"
  artifacts:
    - path: "frontend/src/components/layout/PropertyPanel.tsx"
      provides: "Side panel for node/edge property inspection"
      exports: ["PropertyPanel"]
      min_lines: 40
    - path: "frontend/src/components/results/TableView.tsx"
      provides: "TanStack Table view of query results"
      exports: ["TableView"]
      min_lines: 40
    - path: "frontend/src/components/results/ResultsView.tsx"
      provides: "Container that switches between graph and table view"
      exports: ["ResultsView"]
    - path: "frontend/src/components/results/ResultsBanner.tsx"
      provides: "Result count and truncation notification banner"
      exports: ["ResultsBanner"]
  key_links:
    - from: "frontend/src/components/layout/PropertyPanel.tsx"
      to: "frontend/src/stores/graph.ts"
      via: "reads selectedNodeId/selectedEdgeId"
      pattern: "useGraphStore"
    - from: "frontend/src/components/results/ResultsView.tsx"
      to: "frontend/src/stores/query.ts"
      via: "reads viewMode"
      pattern: "useQueryStore.*viewMode"
    - from: "frontend/src/components/results/TableView.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable hook"
      pattern: "useReactTable"
---

<objective>
Build the property inspection panel, table view, result view toggle, and result count banner.

Purpose: Complete the results interaction layer: users can inspect node/edge properties, switch between graph and table views, and see result set information.
Output: Property panel (shadcn Sheet), TanStack Table view, graph/table toggle, and result truncation banner.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-graph-visualization/01-RESEARCH.md
@.planning/phases/01-foundation-and-graph-visualization/01-CONTEXT.md
@.planning/phases/01-foundation-and-graph-visualization/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-graph-visualization/01-02-SUMMARY.md
@.planning/phases/01-foundation-and-graph-visualization/01-03-SUMMARY.md

<interfaces>
From frontend/src/types/graph.ts:
```typescript
export interface GraphNode { id: string | number; labels: string[]; properties: Record<string, unknown>; label?: string; }
export interface GraphEdge { id: string | number; source: string | number | GraphNode; target: string | number | GraphNode; type: string; properties: Record<string, unknown>; }
export interface GraphData { nodes: GraphNode[]; links: GraphEdge[]; }
export type ViewMode = 'graph' | 'table';
```

From frontend/src/stores/graph.ts:
```typescript
export const useGraphStore: { selectedNodeId: string | number | null; selectedEdgeId: string | number | null; selectNode: (id) => void; selectEdge: (id) => void; clearSelection: () => void; }
```

From frontend/src/stores/query.ts:
```typescript
export const useQueryStore: { viewMode: ViewMode; setViewMode: (mode) => void; toggleViewMode: () => void; }
```

From frontend/src/api/queries.ts:
```typescript
export function useCypherQuery(): UseMutationResult<QueryResponse, ApiError, string>;
```

From frontend/src/components/ui/sheet.tsx:
shadcn Sheet component (Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription)

From frontend/src/components/ui/table.tsx:
shadcn Table component (Table, TableHeader, TableRow, TableHead, TableBody, TableCell)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create property panel and result controls</name>
  <files>
    frontend/src/components/layout/PropertyPanel.tsx,
    frontend/src/components/results/ResultsBanner.tsx
  </files>
  <action>
    1. Create frontend/src/components/layout/PropertyPanel.tsx:
       - Use shadcn Sheet component (SheetContent anchored to "right")
       - Sheet open state controlled by: open when selectedNodeId or selectedEdgeId is not null
       - onOpenChange: when closed, call graphStore.clearSelection()
       - Sheet width: w-80 (~320px) per CONTEXT.md decision
       - Content layout:
         a. Header shows element type badge: "Node" (with label badges) or "Edge" (with relationship type)
         b. For nodes: show all labels as Badge components, then properties table
         c. For edges: show relationship type as Badge, then properties table
         d. Properties displayed as key-value pairs in a two-column layout (key left, value right)
         e. Property values: render strings, numbers, booleans directly; objects/arrays as JSON
       - Find the selected node/edge from graphData (passed as prop or accessed via parent)
       - Accept graphData: GraphData as prop to look up selected element details

    2. Create frontend/src/components/results/ResultsBanner.tsx:
       - Displays result count and truncation info above the results area
       - Props: nodeCount, edgeCount, isLimited (boolean), resultLimit (number)
       - When isLimited: show "Showing {resultLimit} results (query returned more)" with option text
       - When not limited: show "Nodes: {nodeCount} | Edges: {edgeCount}"
       - Small, muted text styling: text-xs text-muted-foreground, p-2 border-b
       - Also include graph/table toggle buttons (two icon buttons: Grid3X3 and Network lucide icons)
       - Toggle buttons use viewMode from useQueryStore, call setViewMode
       - Active view has primary styling, inactive has ghost styling
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 && echo "TYPES OK"</automated>
  </verify>
  <done>
    PropertyPanel opens as right sheet when node/edge selected. Shows element type, labels/type badges, and all properties as key-value pairs. Closes on Sheet close, background click, or Escape. ResultsBanner shows count, truncation info, and graph/table toggle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create table view and wire results view together</name>
  <files>
    frontend/src/components/results/TableView.tsx,
    frontend/src/components/results/ResultsView.tsx,
    frontend/src/App.tsx
  </files>
  <action>
    1. Create frontend/src/components/results/TableView.tsx:
       - Accept graphData: GraphData as prop
       - Use @tanstack/react-table with useReactTable, getCoreRowModel, getPaginationRowModel
       - Display nodes in a table with columns:
         a. ID column
         b. Labels column (comma-separated)
         c. Dynamic property columns: union of all property keys across nodes
       - Use shadcn Table, TableHeader, TableRow, TableHead, TableBody, TableCell components
       - Add simple pagination (Previous/Next buttons) at the bottom using shadcn Button
       - Page size: 20 rows
       - Render property values as strings (JSON.stringify for objects/arrays)

    2. Create frontend/src/components/results/ResultsView.tsx:
       - Container that switches between GraphCanvas and TableView based on viewMode
       - Read viewMode from useQueryStore
       - When 'graph': render GraphCanvas with graphData
       - When 'table': render TableView with graphData
       - Both components receive the same graphData, so toggling does NOT re-execute the query
       - Occupy the full remaining space (flex-1, overflow-hidden)

    3. Update App.tsx:
       - Replace direct GraphCanvas usage with ResultsView
       - Add ResultsBanner between QueryError and ResultsView
       - Add PropertyPanel overlay (always rendered, visibility controlled by selection state)
       - Calculate isLimited: compare result count to settings.resultLimit
       - Pass nodeCount and edgeCount to ResultsBanner
       - Overall layout:
         ```
         AppShell
           Header (with connection status, theme toggle, settings)
           QueryInput
           QueryError (conditionally)
           ResultsBanner (when results exist)
           ResultsView (GraphCanvas or TableView)
           PropertyPanel (Sheet overlay)
         ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 && npm run build 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <done>
    TableView renders node data with ID, labels, and property columns using TanStack Table + shadcn Table. ResultsView toggles between graph and table without re-executing query. PropertyPanel overlays from the right. ResultsBanner shows counts and toggle buttons. Complete results workflow wired in App.tsx.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` completes successfully
- Property panel opens when clicking a node (shows labels + properties)
- Property panel opens when clicking an edge (shows type + properties)
- Property panel closes on Escape or clicking outside
- Graph/table toggle switches view without re-executing query
- Table view shows nodes with ID, labels, and property columns
- Result banner shows node and edge counts
- Truncation message appears when result limit fires
</verification>

<success_criteria>
- Property panel: click node shows labels + properties, click edge shows type + properties
- Panel closes on Escape, background click, or Sheet close button
- Graph/table toggle preserves results (no re-query)
- Table view: paginated, shows all node properties as columns
- Result banner: count display + truncation notification
- All components properly typed and building
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-graph-visualization/01-04-SUMMARY.md`
</output>
