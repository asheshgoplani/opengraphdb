---
phase: 06-demo-datasets-and-live-backend
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - datasets/movies.json
  - datasets/social.json
  - datasets/fraud.json
  - scripts/seed-demo.sh
autonomous: true
requirements:
  - DEMO-03
  - DEMO-04

must_haves:
  truths:
    - "datasets/ directory contains three JSON files (movies.json, social.json, fraud.json) in the {nodes:[], edges:[]} import format"
    - "All node IDs are sequential unsigned integers with no overlapping ranges across files"
    - "movies.json has at least 200 nodes covering real movies, persons, and genres with ACTED_IN, DIRECTED, WROTE, IN_GENRE relationships"
    - "social.json has at least 200 nodes covering users, posts, and groups"
    - "fraud.json has at least 100 nodes covering accounts, transactions, devices, and IPs"
    - "scripts/seed-demo.sh is executable, idempotent, and starts fresh every run"
    - "data/ directory is gitignored"
  artifacts:
    - path: "datasets/movies.json"
      provides: "Movies import dataset with real titles/actors/directors/genres"
      contains: "nodes"
    - path: "datasets/social.json"
      provides: "Social network import dataset"
      contains: "nodes"
    - path: "datasets/fraud.json"
      provides: "Fraud detection import dataset"
      contains: "nodes"
    - path: "scripts/seed-demo.sh"
      provides: "Idempotent seed script"
      contains: "OGDB_DEMO_DB"
  key_links:
    - from: "scripts/seed-demo.sh"
      to: "datasets/*.json"
      via: "ogdb import command"
      pattern: "import.*datasets/"
    - from: "datasets/movies.json edges"
      to: "datasets/movies.json nodes"
      via: "src/dst integer IDs in range 0-499"
      pattern: "\"src\":"
---

<objective>
Create three production-quality demo dataset JSON files and an idempotent seed script. These files are the data foundation for Phase 6: without them the live backend has nothing to serve.

Purpose: Provide rich, realistic demo data that makes OpenGraphDB feel like a real product when evaluators run the seed script and start the server. The movies dataset is the flagship showcase with real well-known titles and actors.

Output: datasets/movies.json (~200+ nodes), datasets/social.json (~200+ nodes), datasets/fraud.json (~100+ nodes), scripts/seed-demo.sh, .gitignore update for data/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-demo-datasets-and-live-backend/06-CONTEXT.md
@.planning/phases/06-demo-datasets-and-live-backend/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create movies.json flagship dataset</name>
  <files>datasets/movies.json</files>
  <action>
Create datasets/movies.json as a JSON object matching the backend POST /import format exactly:

```json
{
  "nodes": [{"id": 0, "labels": ["Movie"], "properties": {..., "_label": "Movie", "_dataset": "movies"}}],
  "edges": [{"src": 50, "dst": 0, "type": "ACTED_IN", "properties": {}}]
}
```

Node ID ranges (non-overlapping, sequential u64 integers):
- Movie nodes: IDs 0 through 49 (up to 50 movies)
- Person nodes: IDs 50 through 249 (up to 200 persons)
- Genre nodes: IDs 250 through 261 (up to 12 genres)

Include these real movies (aim for 40+ movies):
Movies (with `title`, `released`, `tagline`, `_label`, `_dataset` properties):
- The Matrix (1999), The Matrix Reloaded (2003), The Matrix Revolutions (2003)
- The Godfather (1972), The Godfather Part II (1974)
- Pulp Fiction (1994)
- The Dark Knight (2008), Batman Begins (2005)
- Inception (2010), Interstellar (2014)
- Forrest Gump (1994)
- The Shawshank Redemption (1994)
- Jurassic Park (1993), The Lost World: Jurassic Park (1997)
- Titanic (1997)
- Schindler's List (1993)
- Good Will Hunting (1997)
- The Silence of the Lambs (1991)
- Fight Club (1999)
- Top Gun (1986), Top Gun: Maverick (2022)
- Cast Away (2000)
- Philadelphia (1993)
- Saving Private Ryan (1998)
- A Beautiful Mind (2001)
- Gladiator (2000)
- The Lord of the Rings: The Fellowship of the Ring (2001)
- Harry Potter and the Sorcerer's Stone (2001)
- Spider-Man (2002)
- The Avengers (2012)
- Iron Man (2008)
- Star Wars: A New Hope (1977)
- Goodfellas (1990)
- Braveheart (1995)
- Schindler's List was already listed — skip duplicate
- Jerry Maguire (1996)
- A Few Good Men (1992)
- As Good as It Gets (1997)
- Rain Man (1988)
- Tootsie (1982)
- The Truman Show (1998)
- The Green Mile (1999)
- Big (1988)
- You've Got Mail (1998)
- Sleepless in Seattle (1993)

Persons (with `name`, `born`, `_label`, `_dataset` properties):
Directors and actors — real names including:
Actors: Keanu Reeves (1964), Laurence Fishburne (1961), Carrie-Anne Moss (1967), Tom Hanks (1956), Tom Cruise (1962), Meg Ryan (1961), Cuba Gooding Jr. (1968), Leonardo DiCaprio (1974), Matt Damon (1970), Robin Williams (1951), Brad Pitt (1963), Morgan Freeman (1937), Tim Robbins (1958), Jodie Foster (1962), Anthony Hopkins (1937), Jeff Goldblum (1952), Sam Neill (1947), Meryl Streep (1949), Dustin Hoffman (1937), Jack Nicholson (1937), Jim Carrey (1962), Russell Crowe (1964), Mel Gibson (1956), Liam Neeson (1952), Harrison Ford (1942), Robert De Niro (1943), Al Pacino (1940), Marlon Brando (1924), Edward Norton (1969), Bruce Willis (1955), Arnold Schwarzenegger (1947), Sigourney Weaver (1949), Julia Roberts (1967), Sandra Bullock (1964), Denzel Washington (1954), Will Smith (1968), Hugh Jackman (1968), Chris Evans (1981), Robert Downey Jr. (1965), Cate Blanchett (1969), Emma Watson (1990)

Directors: Lana Wachowski (1965), Lilly Wachowski (1967), Francis Ford Coppola (1939), Quentin Tarantino (1963), Christopher Nolan (1970), Robert Zemeckis (1951), Frank Darabont (1959), Steven Spielberg (1946), James Cameron (1954), Cameron Crowe (1957), Gus Van Sant (1952), Jonathan Demme (1944), David Fincher (1962), Tony Scott (1944), Rob Reiner (1947), Peter Jackson (1961), Chris Columbus (1958), Sam Raimi (1959), Joss Whedon (1964), Jon Favreau (1966), George Lucas (1944), Martin Scorsese (1942), Ron Howard (1954), Ridley Scott (1937), Roland Emmerich (1955)

Genres (with `name`, `_label`, `_dataset` properties): Action, Drama, Thriller, Sci-Fi, Crime, Comedy, Romance, Horror, Animation, Documentary, Biography, War

Relationships:
- ACTED_IN: edges from Person IDs to Movie IDs (include `roles` array property where meaningful)
- DIRECTED: edges from Person IDs to Movie IDs
- WROTE: edges from Person IDs to Movie IDs
- IN_GENRE: edges from Movie IDs to Genre IDs

Ensure every movie has at least one DIRECTED edge and at least one IN_GENRE edge. Most movies should have 2-5 ACTED_IN edges. The Matrix trilogy should be fully connected.

CRITICAL: All `id`, `src`, `dst` values MUST be JavaScript numbers (not strings). No string IDs anywhere in this file.
  </action>
  <verify>
    <automated>node -e "const d=require('./datasets/movies.json'); console.log('nodes:', d.nodes.length, 'edges:', d.edges.length); const hasString = d.nodes.some(n => typeof n.id !== 'number'); if(hasString) throw new Error('string IDs found'); console.log('OK')"</automated>
  </verify>
  <done>datasets/movies.json exists with 200+ nodes (movies + persons + genres), all integer IDs in range 0-261, multiple relationship types, real recognizable data</done>
</task>

<task type="auto">
  <name>Task 2: Create social.json, fraud.json, and seed script</name>
  <files>datasets/social.json, datasets/fraud.json, scripts/seed-demo.sh, .gitignore</files>
  <action>
**social.json** — expanded social network dataset with 200+ nodes:

Node ID ranges: User 500-699, Post 700-759, Group 760-779 (all numeric)

Users (60+ users with `name`, `joinedYear`, `bio`, `_label`, `_dataset`):
Include diverse names across tech, creative, academic communities. Examples: Alice Chen, Bob Martinez, Carol Johnson, David Kim, Eva Williams, Frank Brown, Grace Lee, Henry Zhang, Isabel Torres, James Park, Karen Singh, Luis Fernandez, Maria Santos, Nathan Wright, Olivia Davis, Paulo Oliveira, Qing Liu, Rachel Green, Sam Wilson, Tanya Brown, Uma Patel, Victor Nguyen, Wendy Clark, Xavier Moreau, Yasmin Hassan, Zoe Campbell, Alex Rivera, Blake Thompson, Chris Morgan, Diana Lee, Eric Johnson, Fiona Walsh, George Chen, Hannah Kim, Ian Foster, Julia Roberts, Kevin Park, Laura Martinez, Mike Davis, Nina Patel, Oscar Rodriguez, Priya Sharma, Quinn Anderson, Rosa Garcia, Steve Miller, Uma Thurman (different person), Vera Wang (common name), Walt Disney (common name, use Walt Duncan instead), Xander Black, Yuki Tanaka, Zahra Ahmed

Posts (30+ posts with `title`, `likes`, `createdYear`, `_label`, `_dataset`): Graph database topics, distributed systems, ML, open source, DevOps, etc.

Groups (15+ groups with `name`, `memberCount`, `category`, `_label`, `_dataset`): Graph Enthusiasts, Database Engineers, Open Source Contributors, ML Practitioners, DevOps Guild, Frontend Developers, Systems Programmers, Data Scientists, Security Researchers, Cloud Architects, Rust Developers, TypeScript Community, Distributed Systems, Academic Researchers, Graph Theory Club

Relationships: FOLLOWS (user to user), CREATED (user to post), LIKED (user to post), POSTED_IN (post to group), MEMBER_OF (user to group)

Ensure there are multiple FOLLOWS chains of length 3+ to make traversal queries interesting.

---

**fraud.json** — expanded fraud detection dataset with 100+ nodes:

Node ID ranges: Account 1000-1059, Transaction 1100-1149, Device 1200-1214, IP 1300-1314 (all numeric)

Accounts (40+ with `holder`, `balance`, `riskScore`, `accountType`, `_label`, `_dataset`):
Mix of checking/savings account types. High risk accounts (riskScore > 0.7) should share devices and IPs with lower-risk accounts. Include: Nora Adams, Liam Patel, Sofia Reed, Owen Brooks, Mia Turner, Ethan Collins, plus 30+ more names.

Transactions (30+ with `amount`, `timestamp`, `status`, `currency`, `_label`, `_dataset`):
Status values: "completed", "flagged", "pending". Flagged transactions should be high-amount (>$2000) and involve high-risk accounts.

Devices (10+ with `type`, `os`, `_label`, `_dataset`): mobile, laptop, tablet devices
IPs (10+ with `address`, `country`, `_label`, `_dataset`): Mix of US and foreign IPs. Foreign IPs (RU, CN, NG) indicate higher risk.

Relationships: SENT_TO (account to transaction), RECEIVED (account to transaction), USED_DEVICE (account to device), LOGGED_FROM (account to ip), FLAGGED (transaction to account)

Create sharing patterns: at least 3 pairs of accounts sharing the same device, at least 2 pairs sharing the same IP from a foreign country.

---

**scripts/seed-demo.sh** — idempotent bash script:

```bash
#!/usr/bin/env bash
set -euo pipefail

OGDB_DEMO_DB="${OGDB_DEMO_DB:-data/demo.ogdb}"
OGDB_BIN="${OGDB_BIN:-ogdb}"
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
DATASETS_DIR="$ROOT_DIR/datasets"

echo "OpenGraphDB Demo Seed Script"
echo "Database: $OGDB_DEMO_DB"
echo ""

# Idempotent: delete and recreate
if [ -f "$OGDB_DEMO_DB" ]; then
  echo "Removing existing database..."
  rm -f "$OGDB_DEMO_DB" "${OGDB_DEMO_DB}-wal" "${OGDB_DEMO_DB}-meta.json"
fi

mkdir -p "$(dirname "$OGDB_DEMO_DB")"

echo "Creating database..."
"$OGDB_BIN" init "$OGDB_DEMO_DB"

echo "Importing movies dataset..."
"$OGDB_BIN" import "$OGDB_DEMO_DB" "$DATASETS_DIR/movies.json"
echo "  Done."

echo "Importing social dataset..."
"$OGDB_BIN" import "$OGDB_DEMO_DB" "$DATASETS_DIR/social.json"
echo "  Done."

echo "Importing fraud dataset..."
"$OGDB_BIN" import "$OGDB_DEMO_DB" "$DATASETS_DIR/fraud.json"
echo "  Done."

echo ""
echo "Seed complete. Start the server:"
echo "  $OGDB_BIN serve $OGDB_DEMO_DB --http"
echo ""
echo "Then open the frontend at http://localhost:5173/playground?mode=live"
```

Make the script executable: chmod +x scripts/seed-demo.sh

**Gitignore update** — append `data/` to .gitignore if not already present. Check with: grep -q "^data/$" .gitignore || echo "data/" >> .gitignore
  </action>
  <verify>
    <automated>node -e "
const s=require('./datasets/social.json');
const f=require('./datasets/fraud.json');
if(s.nodes.length < 100) throw new Error('social too small: ' + s.nodes.length);
if(f.nodes.length < 60) throw new Error('fraud too small: ' + f.nodes.length);
const sHasString = s.nodes.some(n => typeof n.id !== 'number');
const fHasString = f.nodes.some(n => typeof n.id !== 'number');
if(sHasString) throw new Error('social has string IDs');
if(fHasString) throw new Error('fraud has string IDs');
console.log('social nodes:', s.nodes.length, 'edges:', s.edges.length);
console.log('fraud nodes:', f.nodes.length, 'edges:', f.edges.length);
console.log('OK');
" && test -x scripts/seed-demo.sh && echo "seed script is executable"</automated>
  </verify>
  <done>datasets/social.json (100+ nodes, integer IDs 500-779 range), datasets/fraud.json (60+ nodes, integer IDs 1000-1314 range), scripts/seed-demo.sh (executable, idempotent), data/ in .gitignore</done>
</task>

</tasks>

<verification>
Run from project root:
1. `node -e "const m=require('./datasets/movies.json'); console.log(m.nodes.length, m.edges.length)"` — prints 200+ nodes
2. `node -e "const s=require('./datasets/social.json'); console.log(s.nodes.length)"` — prints 100+ nodes
3. `node -e "const f=require('./datasets/fraud.json'); console.log(f.nodes.length)"` — prints 60+ nodes
4. `bash -n scripts/seed-demo.sh` — bash syntax check passes
5. `test -x scripts/seed-demo.sh && echo executable` — prints executable
</verification>

<success_criteria>
Three import-ready JSON files exist in datasets/ with realistic demo data, all integer node IDs in non-overlapping ranges, and an executable idempotent seed script. Running `./scripts/seed-demo.sh` with a working ogdb binary produces a fully populated demo.ogdb database.
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-datasets-and-live-backend/06-01-SUMMARY.md`
</output>
