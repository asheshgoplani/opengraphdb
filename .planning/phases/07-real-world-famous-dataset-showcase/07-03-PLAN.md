---
phase: 07-real-world-famous-dataset-showcase
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - frontend/src/data/datasets.ts
  - frontend/src/data/datasets.test.ts
  - frontend/src/components/landing/ShowcaseSection.tsx
autonomous: true
requirements: [SHOWCASE-01, SHOWCASE-02, SHOWCASE-03, SHOWCASE-04]

must_haves:
  truths:
    - "DatasetKey type is 'movielens' | 'airroutes' | 'got' | 'wikidata'"
    - "DATASETS record has exactly 4 entries (movielens, airroutes, got, wikidata)"
    - "Old dataset keys (movies, social, fraud) are completely removed"
    - "getDatasetList() returns 4 items with real stats and recognizable names"
    - "ShowcaseSection renders 4 cards in a responsive grid (2x2 on md, 1 col on mobile)"
    - "All dataset tests pass with the new 4 dataset keys"
    - "Landing page showcase heading and description reflect real-world famous datasets"
    - "Playground DatasetSwitcher shows 4 entries"
  artifacts:
    - path: "frontend/src/data/datasets.ts"
      provides: "Updated registry with 4 new datasets replacing old 3"
      exports: ["DatasetKey", "DATASETS", "getDatasetList", "getDatasetQueries", "runDatasetQuery"]
      contains: "movielens.*airroutes.*got.*wikidata"
    - path: "frontend/src/data/datasets.test.ts"
      provides: "Updated tests for 4 new dataset keys"
      contains: "movielens.*airroutes.*got.*wikidata"
    - path: "frontend/src/components/landing/ShowcaseSection.tsx"
      provides: "Updated grid layout for 4 showcase cards"
  key_links:
    - from: "frontend/src/data/datasets.ts"
      to: "frontend/src/data/movieLensGraph.ts"
      via: "import MOVIELENS_SAMPLE, MOVIELENS_QUERIES"
      pattern: "import.*MOVIELENS.*from.*movieLensGraph"
    - from: "frontend/src/data/datasets.ts"
      to: "frontend/src/data/airRoutesGraph.ts"
      via: "import AIR_ROUTES_SAMPLE, AIR_ROUTES_QUERIES"
      pattern: "import.*AIR_ROUTES.*from.*airRoutesGraph"
    - from: "frontend/src/data/datasets.ts"
      to: "frontend/src/data/gotGraph.ts"
      via: "import GOT_SAMPLE, GOT_QUERIES"
      pattern: "import.*GOT.*from.*gotGraph"
    - from: "frontend/src/data/datasets.ts"
      to: "frontend/src/data/wikidataGraph.ts"
      via: "import WIKIDATA_SAMPLE, WIKIDATA_QUERIES"
      pattern: "import.*WIKIDATA.*from.*wikidataGraph"
    - from: "frontend/src/components/landing/ShowcaseSection.tsx"
      to: "frontend/src/data/datasets.ts"
      via: "getDatasetList()"
      pattern: "getDatasetList"
---

<objective>
Wire the 4 new dataset modules into the central registry, update all tests, and adjust the landing page showcase grid from 3 columns to a 4-card layout.

Purpose: This is the integration plan that connects the new offline fallback modules (from Plan 02) to the rest of the frontend. After this plan, the playground, landing page, and all dataset-dependent components work with the 4 famous datasets instead of the old synthetic ones.

Output: Updated datasets.ts registry, passing tests, responsive 4-card showcase grid
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-real-world-famous-dataset-showcase/07-CONTEXT.md
@.planning/phases/07-real-world-famous-dataset-showcase/07-02-SUMMARY.md

@frontend/src/data/datasets.ts (current registry to replace)
@frontend/src/data/datasets.test.ts (current tests to update)
@frontend/src/components/landing/ShowcaseSection.tsx (current 3-column grid)

<interfaces>
<!-- Exports from Plan 02 modules that this plan consumes -->

From frontend/src/data/movieLensGraph.ts:
```typescript
export const MOVIELENS_SAMPLE: GraphData
export const MOVIELENS_QUERIES: GuidedQuery[]
```

From frontend/src/data/airRoutesGraph.ts:
```typescript
export const AIR_ROUTES_SAMPLE: GraphData
export const AIR_ROUTES_QUERIES: GuidedQuery[]
```

From frontend/src/data/gotGraph.ts:
```typescript
export const GOT_SAMPLE: GraphData
export const GOT_QUERIES: GuidedQuery[]
```

From frontend/src/data/wikidataGraph.ts:
```typescript
export const WIKIDATA_SAMPLE: GraphData
export const WIKIDATA_QUERIES: GuidedQuery[]
```

From frontend/src/data/datasets.ts (current, to be replaced):
```typescript
export type DatasetKey = 'movies' | 'social' | 'fraud'
export interface GuidedQuery { key, label, description, cypher, expectedResultCount, filterFn, liveDescriptor?, category? }
export interface DatasetMeta { key, name, description, nodeCount, linkCount, labels }
export const DATASETS: Record<DatasetKey, DatasetEntry>
export function getDatasetList(): DatasetMeta[]
export function getDatasetQueries(key: DatasetKey): GuidedQuery[]
export function runDatasetQuery(datasetKey: DatasetKey, queryKey: string): GraphData
```

From frontend/src/components/landing/ShowcaseSection.tsx:
- Uses `getDatasetList()` and `runDatasetQuery()` from datasets.ts
- Currently renders a 3-column grid: `grid-cols-1 md:grid-cols-3`
- `CARD_DELAY_CLASSES` array has 3 entries for staggered animation

From frontend/src/components/landing/ShowcaseCard.tsx:
- Accepts `datasetKey: DatasetKey` prop (will automatically work with new keys)
- No changes needed to ShowcaseCard itself
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update datasets.ts registry and remove old modules</name>
  <files>
    frontend/src/data/datasets.ts
  </files>
  <action>
    Update `datasets.ts` to replace the old 3 synthetic datasets with the 4 new famous datasets.

    **Changes:**

    1. **Imports**: Replace old imports (`FRAUD_QUERIES, FRAUD_SAMPLE from './fraudGraph.js'`, `MOVIES_SAMPLE from './sampleGraph.js'`, `SOCIAL_QUERIES, SOCIAL_SAMPLE from './socialGraph.js'`) with new imports:
       ```typescript
       import { MOVIELENS_SAMPLE, MOVIELENS_QUERIES } from './movieLensGraph.js'
       import { AIR_ROUTES_SAMPLE, AIR_ROUTES_QUERIES } from './airRoutesGraph.js'
       import { GOT_SAMPLE, GOT_QUERIES } from './gotGraph.js'
       import { WIKIDATA_SAMPLE, WIKIDATA_QUERIES } from './wikidataGraph.js'
       ```

    2. **DatasetKey type**: Change from `'movies' | 'social' | 'fraud'` to `'movielens' | 'airroutes' | 'got' | 'wikidata'`

    3. **DATASETS record**: Replace all 3 entries with 4 new entries:
       - `movielens`: data=MOVIELENS_SAMPLE, queries=MOVIELENS_QUERIES, meta with name="MovieLens 25M", description="Industry-standard movie recommendation dataset with 8,000 movies and genre classifications from GroupLens Research"
       - `airroutes`: data=AIR_ROUTES_SAMPLE, queries=AIR_ROUTES_QUERIES, meta with name="Air Routes Network", description="Global airport network with 3,500 airports and 50,000 routes from Kelvin Lawrence's Practical Gremlin dataset"
       - `got`: data=GOT_SAMPLE, queries=GOT_QUERIES, meta with name="Game of Thrones", description="Character interaction network across 8 seasons with weighted relationships from Andrew Beveridge's research"
       - `wikidata`: data=WIKIDATA_SAMPLE, queries=WIKIDATA_QUERIES, meta with name="Nobel Prize Knowledge Graph", description="Nobel Prize laureates connected to countries, institutions, and prize categories from the Nobel Prize Foundation API"

    4. **Remove old queries**: Delete the `MOVIES_QUERIES` constant array that was defined inline in datasets.ts (it was the movies dataset's queries defined directly in this file, not imported). Also remove the `buildActorCollaborationsSubgraph` helper that was only used by MOVIES_QUERIES.

    5. **Keep all utility functions**: `cloneNode`, `cloneLink`, `cloneGraphData`, `buildRelationshipSubgraph`, `toNodeId`, `buildDatasetMeta`, `cloneGuidedQuery`, `getDatasetList`, `getDatasetQueries`, `runDatasetQuery` remain unchanged. They are generic and work with any dataset.

    6. **Delete old data files**: Remove the old TypeScript data modules that are no longer referenced:
       - `frontend/src/data/sampleGraph.ts` (was MOVIES_SAMPLE)
       - `frontend/src/data/socialGraph.ts` (was SOCIAL_SAMPLE)
       - `frontend/src/data/fraudGraph.ts` (was FRAUD_SAMPLE)
       - `frontend/src/data/sampleGraph.test.ts` (old movies test)
       Use `trash` command to delete (never use `rm`).

    The rest of the file stays the same. The `ShowcaseSection`, `ShowcaseCard`, `DatasetSwitcher`, and `PlaygroundPage` components all use `getDatasetList()` and `getDatasetQueries()` which will automatically pick up the new datasets without any changes to those components.
  </action>
  <verify>
    <automated>cd /Users/ashesh/opengraphdb/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    datasets.ts has DatasetKey = 'movielens' | 'airroutes' | 'got' | 'wikidata'. DATASETS record has 4 entries with descriptive names. Old imports and inline queries removed. Old data files (sampleGraph.ts, socialGraph.ts, fraudGraph.ts, sampleGraph.test.ts) deleted. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests and ShowcaseSection grid layout</name>
  <files>
    frontend/src/data/datasets.test.ts
    frontend/src/components/landing/ShowcaseSection.tsx
  </files>
  <action>
    **Update datasets.test.ts:**

    Replace all references to old dataset keys with new ones. The test structure stays identical; only key names and label assertions change.

    1. In "getDatasetList returns exactly..." test: Change `list.length` from 3 to 4. Change `Set(['movies', 'social', 'fraud'])` to `Set(['movielens', 'airroutes', 'got', 'wikidata'])`.

    2. In "every dataset has guided queries including all" test: Change `keys` array from `['movies', 'social', 'fraud']` to `['movielens', 'airroutes', 'got', 'wikidata']`.

    3. In "runDatasetQuery('all') returns complete data..." test: Change `keys` array to `['movielens', 'airroutes', 'got', 'wikidata']`.

    4. In "relationship-filtered queries return orphan-free connected subgraphs" test: Change `keys` array to `['movielens', 'airroutes', 'got', 'wikidata']`.

    5. In "dataset labels include expected domain entities" test: Replace all old label assertions with new ones:
       - `movielens` should have labels `Movie` and `Genre`
       - `airroutes` should have labels `Airport`, `Country`, `Continent`
       - `got` should have labels `Character` and `Season`
       - `wikidata` should have labels `Laureate`, `Category`, `Country`

    **Update ShowcaseSection.tsx:**

    1. Change the grid from `md:grid-cols-3` to `md:grid-cols-2 lg:grid-cols-4` so 4 cards fit on large screens and wrap to 2x2 on medium screens.

    2. Update `CARD_DELAY_CLASSES` array to have 4 entries: `['animate-delay-100', 'animate-delay-200', 'animate-delay-300', 'animate-delay-[400ms]']` (or use Tailwind's built-in delay classes if available).

    3. Update the section heading from "Real-World Knowledge Graphs" to "Famous Graph Datasets" (or keep as-is if it already works; the key is that the description text should reference the 4 industry-standard datasets).

    4. Update the description paragraph to: "Explore the same industry-standard datasets used by Neo4j, TigerGraph, and Amazon Neptune. From movie recommendations to airport networks, these benchmarks demonstrate real graph database capabilities."
  </action>
  <verify>
    <automated>cd /Users/ashesh/opengraphdb/frontend && npx tsx --test src/data/datasets.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 5 dataset tests pass with the new 4 dataset keys. Label assertions verify movielens has Movie/Genre, airroutes has Airport/Country/Continent, got has Character/Season, wikidata has Laureate/Category/Country. ShowcaseSection renders a 4-card responsive grid with staggered animation delays. Landing page description references industry-standard benchmark datasets.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npx tsx --test src/data/datasets.test.ts` all 5 tests pass
3. No references to old dataset keys ('movies', 'social', 'fraud') remain in datasets.ts or tests
4. Old data files (sampleGraph.ts, socialGraph.ts, fraudGraph.ts) are removed
5. ShowcaseSection grid accommodates 4 cards without overflow
6. getDatasetList() returns exactly 4 entries
</verification>

<success_criteria>
- DatasetKey union type has exactly 4 members: movielens, airroutes, got, wikidata
- DATASETS record has 4 entries with meaningful names and descriptions
- All dataset tests pass (5 test cases)
- ShowcaseSection grid layout supports 4 cards responsively
- Old synthetic dataset modules are deleted
- No TypeScript compilation errors
- Playground DatasetSwitcher automatically shows 4 datasets (via getDatasetList)
</success_criteria>

<output>
After completion, create `.planning/phases/07-real-world-famous-dataset-showcase/07-03-SUMMARY.md`
</output>
