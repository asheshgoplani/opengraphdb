---
phase: 05-frontend-polish-and-showcase
plan: 05
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/Header.tsx
  - frontend/src/components/results/ResultsBanner.tsx
  - frontend/src/components/results/ResultsView.tsx
  - frontend/src/components/layout/PropertyPanel.tsx
  - frontend/src/components/layout/ConnectionStatus.tsx
  - frontend/src/components/layout/SettingsDialog.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "App page (/app) header uses glass effect (bg-card/80 backdrop-blur-sm) matching landing page nav aesthetic"
    - "Header typography and spacing are consistent with the polished landing page"
    - "ResultsBanner has refined spacing, subtle border, and consistent badge styling"
    - "ResultsView graph/table toggle buttons have proper hover states and active indicators"
    - "PropertyPanel has refined typography, consistent spacing, and glass sidebar effect"
    - "ConnectionStatus indicator uses the same green pulsing dot pattern as ConnectionBadge"
    - "SettingsDialog has consistent form styling with proper focus rings"
    - "All components honor dark mode with the refined palette"
    - "Empty state ('Run a query to see results') has polished typography and spacing"
    - "Transitions between states (loading, results, error) are smooth"
  artifacts:
    - path: "frontend/src/components/layout/AppShell.tsx"
      provides: "Polished app layout wrapper"
    - path: "frontend/src/components/layout/Header.tsx"
      provides: "Refined header with glass effect"
    - path: "frontend/src/components/results/ResultsBanner.tsx"
      provides: "Polished results summary bar"
    - path: "frontend/src/components/results/ResultsView.tsx"
      provides: "Refined graph/table view with toggle"
    - path: "frontend/src/components/layout/PropertyPanel.tsx"
      provides: "Polished property inspection sidebar"
    - path: "frontend/src/components/layout/ConnectionStatus.tsx"
      provides: "Green pulsing dot connection indicator"
    - path: "frontend/src/components/layout/SettingsDialog.tsx"
      provides: "Refined settings dialog"
---

<objective>
Polish the /app page (full explorer) to match the same visual quality bar as the landing page and playground.

Purpose: Ensure the full app experience is consistent with the polished landing page and playground. Every component on /app should feel cohesive and professional.

Output: All /app components upgraded with refined spacing, glass effects, consistent color palette, and smooth transitions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-polish-and-showcase/05-CONTEXT.md

<interfaces>
From frontend/src/components/layout/AppShell.tsx (CURRENT):
```typescript
// Simple flex column layout wrapper
```

From frontend/src/components/layout/Header.tsx (CURRENT):
```typescript
// Basic header with title, settings button, theme toggle
```

From frontend/src/components/layout/ConnectionStatus.tsx (CURRENT):
```typescript
// Basic connected/disconnected text indicator
```

From frontend/src/components/results/ResultsBanner.tsx (CURRENT):
```typescript
interface ResultsBannerProps {
  nodeCount: number; edgeCount: number; isLimited: boolean;
  resultLimit: number; queryResponse: CypherQueryResponse | null;
}
```

From frontend/src/components/results/ResultsView.tsx:
```typescript
// Graph view / Table view toggle with GraphCanvas and TableView
```

From frontend/src/components/layout/PropertyPanel.tsx:
```typescript
// Sheet-based side panel showing node/edge properties
```

Polished patterns established in plan 05-01:
- Glass effect: bg-background/80 backdrop-blur-md
- Refined dark palette: bg #0f0f1a, text slate-100
- Animation utilities available
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish Header with glass effect and refined styling</name>
  <files>frontend/src/components/layout/Header.tsx</files>
  <action>
1. Read `frontend/src/components/layout/Header.tsx`.

2. Upgrade styling:
   - Add glass effect: `bg-card/80 backdrop-blur-sm border-b`
   - "OpenGraphDB" title in left: add small graph icon (Lucide `Share2`) before text
   - Add "Explorer" subtitle/badge next to the title to differentiate from playground
   - Ensure buttons (settings, theme toggle) have consistent size and spacing
   - Add a subtle separator between left group and right group
   - Ensure connection status is visible in the header
   - Height: h-12 or h-14 for comfortable spacing
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>Header has glass effect, graph icon, Explorer badge, consistent button sizing.</done>
</task>

<task type="auto">
  <name>Task 2: Polish ConnectionStatus with green pulsing dot</name>
  <files>frontend/src/components/layout/ConnectionStatus.tsx</files>
  <action>
1. Read `frontend/src/components/layout/ConnectionStatus.tsx`.

2. Upgrade to use the same pulsing dot pattern as ConnectionBadge:
   - Connected: green pulsing dot + "Connected" text + server URL in muted text
   - Disconnected: red static dot + "Disconnected" text
   - Wrap in a rounded-full border container: `flex items-center gap-2 rounded-full border px-3 py-1 text-xs`
   - Green dot: `<span className="relative flex h-2 w-2"><span className="animate-ping absolute h-full w-full rounded-full bg-emerald-400 opacity-75" /><span className="relative h-2 w-2 rounded-full bg-emerald-500" /></span>`
   - Red dot: `<span className="h-2 w-2 rounded-full bg-red-500" />`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>ConnectionStatus uses green pulsing dot for connected, red static dot for disconnected, in a pill-shaped container.</done>
</task>

<task type="auto">
  <name>Task 3: Polish ResultsBanner and ResultsView</name>
  <files>frontend/src/components/results/ResultsBanner.tsx, frontend/src/components/results/ResultsView.tsx</files>
  <action>
1. Read `frontend/src/components/results/ResultsBanner.tsx`.
   - Refine spacing: consistent px-4 py-2
   - Use Badge components for node/edge counts instead of plain text
   - Add subtle border-b and bg-muted/30 background
   - Export buttons: add proper hover states and icons
   - Limited results warning: use amber Badge instead of plain text

2. Read `frontend/src/components/results/ResultsView.tsx`.
   - Graph/table toggle: use proper button group styling with rounded-lg border wrapper
   - Active toggle: `bg-primary text-primary-foreground`
   - Inactive toggle: `hover:bg-accent`
   - Add smooth transition when switching views (opacity fade)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>ResultsBanner uses Badges for counts with refined spacing. ResultsView has proper toggle group with smooth transitions.</done>
</task>

<task type="auto">
  <name>Task 4: Polish PropertyPanel and SettingsDialog</name>
  <files>frontend/src/components/layout/PropertyPanel.tsx, frontend/src/components/layout/SettingsDialog.tsx</files>
  <action>
1. Read `frontend/src/components/layout/PropertyPanel.tsx`.
   - Sheet overlay: ensure dark mode overlay is visible
   - Property key-value pairs: use consistent `text-sm` with `text-muted-foreground` for keys and `text-foreground` for values
   - Add proper padding and spacing
   - Node/edge type badge at top of panel
   - Empty state: "Click a node or edge to view properties"

2. Read `frontend/src/components/layout/SettingsDialog.tsx`.
   - Form inputs: ensure consistent focus ring styling
   - Labels: text-sm font-medium
   - Descriptions: text-xs text-muted-foreground below inputs
   - Proper spacing between form groups
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>PropertyPanel has refined property display with type badges. SettingsDialog has consistent form styling.</done>
</task>

<task type="auto">
  <name>Task 5: Polish AppShell and empty state</name>
  <files>frontend/src/components/layout/AppShell.tsx, frontend/src/App.tsx</files>
  <action>
1. Read `frontend/src/components/layout/AppShell.tsx`.
   - Ensure it provides proper flex layout with `min-h-screen`
   - Background: `bg-background`

2. Read `frontend/src/App.tsx`.
   - Polish the empty state ("Run a query to see results"):
     - Wrap in a card-like container with border and rounded-lg
     - Add a subtle graph icon (Lucide `Workflow` or `GitBranch`) above the text
     - Title: "Run a query to see results" in text-lg font-medium
     - Subtitle: "Try the example below" in text-sm text-muted-foreground
     - Code block: more polished with rounded-lg bg-muted px-4 py-2 font-mono text-sm
     - Add animate-fade-in for smooth entrance
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>App shell has proper base styles. Empty state has icon, refined text, polished code block with fade-in animation.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript compiles without errors
2. `cd frontend && npm run build` — Build succeeds
3. `cd frontend && npm run test:unit` — All existing tests pass
4. Visual: /app header has glass effect and connection indicator
5. Visual: Results banner uses badges for counts
6. Visual: Graph/table toggle has proper button group styling
7. Visual: Property panel has refined typography
8. Visual: Empty state has icon and polished layout
9. Visual: All components look consistent in dark mode
</verification>

<success_criteria>
- Header uses glass effect matching landing page nav
- ConnectionStatus uses green pulsing dot pattern
- ResultsBanner uses Badge components for counts
- ResultsView toggle has button group styling
- PropertyPanel has refined property display
- Empty state has icon, polished text, fade-in animation
- All components consistent in dark mode
- Build succeeds, TS compiles, tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-polish-and-showcase/05-05-SUMMARY.md`
</output>
