---
phase: 05-frontend-polish-and-showcase
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - frontend/src/pages/LandingPage.tsx
  - frontend/src/components/landing/LandingNav.tsx
  - frontend/src/components/landing/HeroSection.tsx
  - frontend/src/components/landing/HeroGraphBackground.tsx
  - frontend/src/components/landing/ShowcaseSection.tsx
  - frontend/src/components/landing/ShowcaseCard.tsx
  - frontend/src/components/landing/FeaturesSection.tsx
  - frontend/src/components/landing/GettingStartedSection.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Landing page has a polished sticky nav with 'OpenGraphDB' wordmark, section scroll links, and Playground/App buttons with blur backdrop"
    - "Hero section renders a live animated force-directed graph behind the text using a lightweight canvas component"
    - "Hero background graph uses a small subset of nodes (8-12) that gently float with low alpha decay for continuous motion"
    - "Hero background has reduced opacity and does not interfere with text readability"
    - "Showcase section displays 3 cards for Movies/Social/Fraud datasets with interactive mini-graph previews"
    - "Each showcase card shows dataset name, description, node/edge counts, and unique label badges"
    - "Mini-graphs in showcase cards are animated (physics simulation running) with hover highlighting connections"
    - "Clicking a showcase card navigates to /playground?dataset={key}"
    - "Features section cards have hover scale transitions and refined shadows"
    - "Getting started section has polished code blocks with copy-to-clipboard"
    - "All landing page sections use staggered fadeIn/slideUp animations on scroll using IntersectionObserver"
    - "Footer is refined with consistent spacing"
    - "Everything looks polished in both light and dark mode"
  artifacts:
    - path: "frontend/src/components/landing/LandingNav.tsx"
      provides: "Polished sticky navigation bar"
      exports: ["LandingNav"]
    - path: "frontend/src/components/landing/HeroGraphBackground.tsx"
      provides: "Animated force-directed graph canvas for hero background"
      exports: ["HeroGraphBackground"]
    - path: "frontend/src/components/landing/HeroSection.tsx"
      provides: "Hero section with live graph background"
      exports: ["HeroSection"]
    - path: "frontend/src/components/landing/ShowcaseSection.tsx"
      provides: "Knowledge graph showcase section with 3 dataset cards"
      exports: ["ShowcaseSection"]
    - path: "frontend/src/components/landing/ShowcaseCard.tsx"
      provides: "Individual showcase card with mini-graph preview"
      exports: ["ShowcaseCard"]
    - path: "frontend/src/components/landing/FeaturesSection.tsx"
      provides: "Polished features grid with hover effects"
      exports: ["FeaturesSection"]
    - path: "frontend/src/components/landing/GettingStartedSection.tsx"
      provides: "Getting started with copy-to-clipboard code blocks"
      exports: ["GettingStartedSection"]
    - path: "frontend/src/pages/LandingPage.tsx"
      provides: "Assembled landing page with all sections"
  key_links:
    - from: "frontend/src/pages/LandingPage.tsx"
      to: "frontend/src/components/landing/LandingNav.tsx"
      via: "Renders LandingNav as sticky header"
      pattern: "LandingNav"
    - from: "frontend/src/components/landing/ShowcaseSection.tsx"
      to: "frontend/src/data/datasets.ts"
      via: "Uses getDatasetList() for showcase card data"
      pattern: "getDatasetList"
    - from: "frontend/src/components/landing/ShowcaseCard.tsx"
      to: "frontend/src/data/datasets.ts"
      via: "Uses runDatasetQuery for mini-graph data"
      pattern: "runDatasetQuery"
---

<objective>
Redesign the landing page to production quality with an animated graph hero, knowledge graph showcase section, and polished sections throughout.

Purpose: Transform the basic landing page into an impressive, Linear/Vercel-quality page that builds trust with evaluators by showing real-world knowledge graph examples.

Output: Polished landing page with animated hero, 3 interactive showcase cards, refined features and getting started sections, and staggered scroll animations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-polish-and-showcase/05-CONTEXT.md

<interfaces>
From frontend/src/data/datasets.ts (created in plan 05-02):
```typescript
export type DatasetKey = 'movies' | 'social' | 'fraud'
export interface DatasetMeta {
  key: DatasetKey; name: string; description: string;
  nodeCount: number; linkCount: number; labels: string[];
}
export interface GuidedQuery { key: string; label: string; description: string; cypher: string; filterFn: (data: GraphData) => GraphData; }
export function getDatasetList(): DatasetMeta[]
export function runDatasetQuery(datasetKey: DatasetKey, queryKey: string): GraphData
```

From frontend/src/components/graph/GraphCanvas.tsx (enhanced in plan 05-01):
- GraphCanvas component with polished rendering, legend overlay

From frontend/src/types/graph.ts:
```typescript
export interface GraphData { nodes: GraphNode[]; links: GraphEdge[]; }
```

Existing landing components (will be modified):
- HeroSection.tsx: Basic text hero
- FeaturesSection.tsx: 4 feature cards
- GettingStartedSection.tsx: 3 code steps

UI components available: Button, Card, CardHeader, CardTitle, CardDescription, CardContent, Badge

CSS animations available (from plan 05-01): animate-fade-in, animate-slide-up, animate-slide-in, animate-scale-in, animate-delay-100 through animate-delay-500, animate-fill-both
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LandingNav sticky navigation</name>
  <files>frontend/src/components/landing/LandingNav.tsx</files>
  <action>
1. Create `frontend/src/components/landing/LandingNav.tsx`:
   - Sticky nav at top with z-50
   - Use glass effect: `bg-background/80 backdrop-blur-md border-b`
   - Left: "OpenGraphDB" wordmark text in font-semibold with a small graph icon from Lucide (e.g., `Share2` or `GitBranch` icon)
   - Center (hidden on mobile): anchor links "Features", "Use Cases", "Get Started" that smooth-scroll to section IDs
   - Right: "Playground" ghost button + "Open App" primary button, both Link to their routes
   - Max-width container consistent with landing page (max-w-6xl)
   - Height: h-14 with flex items-center
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>LandingNav renders as a sticky glass-effect nav with wordmark, section links, and route buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Create HeroGraphBackground animated canvas</name>
  <files>frontend/src/components/landing/HeroGraphBackground.tsx</files>
  <action>
1. Create `frontend/src/components/landing/HeroGraphBackground.tsx`:
   - A lightweight component that renders a small force-directed graph behind the hero text
   - Use `react-force-graph-2d` directly (like GraphCanvas but stripped down for background use)
   - Generate a small dataset inline: 8-12 random nodes with 10-15 links, abstract labels like "A", "B", "C"
   - Key settings for background feel:
     - `d3AlphaDecay={0.005}` — very slow settling so nodes keep floating
     - `d3AlphaMin={0.001}` — never fully stops
     - `d3VelocityDecay={0.15}` — low damping, fluid motion
     - `enableNodeDrag={false}` — no interaction
     - `enableZoomInteraction={false}` — no zoom
     - `enablePanInteraction={false}` — no pan
   - Node rendering: small circles (radius 3-4) with soft glow, using LABEL_COLORS from NodeRenderer
   - Edge rendering: thin lines (0.5px) with low opacity
   - Container is absolutely positioned, fills the hero section, has `opacity-20` in light mode and `opacity-15` in dark mode (very subtle)
   - Use `pointer-events-none` so it doesn't capture clicks
   - Canvas background matches page bg (transparent)
   - Use ResizeObserver for responsive width/height (same pattern as GraphCanvas)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>HeroGraphBackground renders a subtle animated graph with floating nodes. Non-interactive, low opacity, pointer-events-none.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign HeroSection with animated background</name>
  <files>frontend/src/components/landing/HeroSection.tsx</files>
  <action>
1. Read existing `frontend/src/components/landing/HeroSection.tsx`.

2. Rewrite with animated graph background:
   ```tsx
   import { HeroGraphBackground } from './HeroGraphBackground'
   ```
   - Outer section: `relative overflow-hidden py-24 sm:py-36`
   - Inside: `<HeroGraphBackground />` positioned absolutely behind content
   - Content div: `relative z-10` (above the background)
   - Title: larger font `text-5xl sm:text-7xl`, font-bold, tracking-tight
   - Subtitle: `text-lg sm:text-xl text-muted-foreground max-w-2xl mx-auto`
   - CTA buttons: larger size with proper spacing
   - Add subtle gradient overlay at bottom of section for smooth transition to next section:
     `<div className="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-background to-transparent" />`
   - Apply animate-fade-in + animate-fill-both to the content for entrance animation
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>HeroSection shows live animated graph background with large bold typography and gradient fade to next section.</done>
</task>

<task type="auto">
  <name>Task 4: Create ShowcaseCard with mini-graph preview</name>
  <files>frontend/src/components/landing/ShowcaseCard.tsx</files>
  <action>
1. Create `frontend/src/components/landing/ShowcaseCard.tsx`:
   ```typescript
   interface ShowcaseCardProps {
     datasetKey: DatasetKey
     name: string
     description: string
     nodeCount: number
     linkCount: number
     labels: string[]
     graphData: GraphData
   }
   ```

   - Uses Card component from shadcn/ui
   - Top portion: mini-graph preview using ForceGraph2D:
     - Fixed height (200px) within the card
     - Uses polished node rendering (import from NodeRenderer)
     - `d3AlphaDecay={0.008}` — slow settle for visible motion
     - `enableNodeDrag={false}`, `enableZoomInteraction={false}`, `enablePanInteraction={false}`
     - Node hover: highlight connected nodes/edges (use `onNodeHover` to track hovered node, conditionally brighten connected elements)
     - Tooltip on hover showing node properties (use a simple div overlay positioned at cursor)
     - Canvas bg matches card bg
   - Bottom portion: text content
     - Dataset name as CardTitle
     - Description as CardDescription
     - Stats row: "{nodeCount} nodes · {linkCount} relationships" in muted text
     - Label badges: map labels to Badge components with colored dots
   - Whole card is wrapped in `<Link to={/playground?dataset=${datasetKey}}>`
   - Card has hover:scale-[1.02] transition-transform duration-200 for subtle lift effect
   - Card has `group` class for group-hover effects on the "Explore →" text
   - Bottom has "Explore in Playground →" text that shifts right on hover with group-hover:translate-x-1
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>ShowcaseCard renders mini-graph with hover highlighting, dataset metadata, label badges, and click-to-playground navigation.</done>
</task>

<task type="auto">
  <name>Task 5: Create ShowcaseSection with dataset cards</name>
  <files>frontend/src/components/landing/ShowcaseSection.tsx</files>
  <action>
1. Create `frontend/src/components/landing/ShowcaseSection.tsx`:
   - Import `getDatasetList`, `runDatasetQuery` from `@/data/datasets`
   - Import `ShowcaseCard` from `./ShowcaseCard`
   - Section has id="use-cases" for nav scroll targeting
   - Section header: "Real-World Knowledge Graphs" title + "OpenGraphDB powers graph workloads across industries" subtitle
   - Mention recognizable datasets: "From movie recommendations to fraud detection, explore how graph databases model complex relationships."
   - Grid: `grid grid-cols-1 md:grid-cols-3 gap-6`
   - For each dataset from `getDatasetList()`:
     - Get all data with `runDatasetQuery(dataset.key, 'all')`
     - Render `<ShowcaseCard>` with the data
   - Use staggered animation: each card gets `animate-slide-up animate-fill-both` with increasing delay (animate-delay-100, animate-delay-200, animate-delay-300)
   - Wrap in IntersectionObserver to only animate when scrolled into view (use a simple `useInView` hook or inline observer)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>ShowcaseSection renders 3 dataset cards in a grid with staggered scroll-triggered animations.</done>
</task>

<task type="auto">
  <name>Task 6: Polish FeaturesSection with hover effects and refined cards</name>
  <files>frontend/src/components/landing/FeaturesSection.tsx</files>
  <action>
1. Read existing `frontend/src/components/landing/FeaturesSection.tsx`.

2. Upgrade:
   - Add id="features" for nav scroll targeting
   - Section header above the grid: "Built for Performance" title + "Every component optimized for graph-native workloads" subtitle
   - Cards: add `hover:shadow-lg hover:shadow-primary/5 transition-all duration-200 hover:-translate-y-0.5`
   - Icon: wrap in a `rounded-lg bg-primary/10 p-2` container for a polished icon treatment
   - Increase card padding slightly
   - Use animate-slide-up with staggered delays per card
   - Add IntersectionObserver trigger for scroll animation
   - Ensure cards look great in dark mode (check border contrast, shadow visibility)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>FeaturesSection has polished cards with hover lift, icon backgrounds, section header, and scroll animations.</done>
</task>

<task type="auto">
  <name>Task 7: Polish GettingStartedSection with copy-to-clipboard and refined code blocks</name>
  <files>frontend/src/components/landing/GettingStartedSection.tsx</files>
  <action>
1. Read existing `frontend/src/components/landing/GettingStartedSection.tsx`.

2. Upgrade:
   - Add id="get-started" for nav scroll targeting
   - Each code block gets a "Copy" button (Lucide `Copy` / `Check` icon) positioned in top-right corner
   - Copy functionality: `navigator.clipboard.writeText(command)` with visual feedback (icon switches to Check for 2s)
   - Code blocks: use `bg-slate-950 text-slate-50` (always dark for code) with `rounded-xl` and slightly more padding
   - Step numbers rendered as styled circles: `rounded-full bg-primary text-primary-foreground w-7 h-7 flex items-center justify-center text-sm font-medium`
   - Staggered slide-up animation on scroll
   - Add a subtle left border accent on each step card
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>GettingStartedSection has always-dark code blocks with copy-to-clipboard, styled step numbers, and scroll animations.</done>
</task>

<task type="auto">
  <name>Task 8: Assemble redesigned LandingPage</name>
  <files>frontend/src/pages/LandingPage.tsx</files>
  <action>
1. Read existing `frontend/src/pages/LandingPage.tsx`.

2. Rewrite with new component structure:
   ```tsx
   import { LandingNav } from '@/components/landing/LandingNav'
   import { HeroSection } from '@/components/landing/HeroSection'
   import { ShowcaseSection } from '@/components/landing/ShowcaseSection'
   import { FeaturesSection } from '@/components/landing/FeaturesSection'
   import { GettingStartedSection } from '@/components/landing/GettingStartedSection'
   ```
   - Replace the old header with `<LandingNav />`
   - Section order: HeroSection → ShowcaseSection → FeaturesSection → GettingStartedSection → Footer
   - Footer: refine with proper spacing, subtle border-t, copyright text
   - Add `scroll-smooth` to the root div for smooth anchor scrolling
   - Ensure `min-h-screen bg-background text-foreground` base classes
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>LandingPage assembles all polished sections: Nav → Hero (with graph bg) → Showcase (3 dataset cards) → Features → Getting Started → Footer.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript compiles without errors
2. `cd frontend && npm run build` — Build succeeds
3. `cd frontend && npm run test:unit` — All existing tests still pass
4. Visual: Landing page hero has animated graph background with bold text
5. Visual: Showcase section shows 3 interactive mini-graph cards
6. Visual: Clicking a showcase card navigates to /playground?dataset={key}
7. Visual: Features and getting started sections have hover effects and scroll animations
8. Visual: Dark mode looks polished across all sections
</verification>

<success_criteria>
- Sticky nav with wordmark, section links, and route buttons
- Hero has live animated graph background, not interfering with text
- 3 showcase cards show mini-graphs for movies, social, fraud datasets
- Mini-graphs animate and highlight on hover
- Showcase cards navigate to /playground?dataset={key} on click
- Features cards have hover lift effect and icon backgrounds
- Getting started has copy-to-clipboard and always-dark code blocks
- All sections animate in on scroll
- Both light and dark mode look polished
- Build succeeds, TS compiles, existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-polish-and-showcase/05-03-SUMMARY.md`
</output>
