---
phase: 05-frontend-polish-and-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tailwind.config.js
  - frontend/src/index.css
  - frontend/src/components/graph/NodeRenderer.ts
  - frontend/src/components/graph/useGraphColors.ts
  - frontend/src/components/graph/GraphCanvas.tsx
  - frontend/src/components/graph/GraphLegend.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Node rendering uses radial gradients with lighter center and darker edge for a polished 3D-like appearance"
    - "Nodes have a subtle outer glow effect using canvas shadow blur matching the node's label color"
    - "Node labels use proper font weight and contrast with text-shadow for readability over any background"
    - "Node radius scales based on connection count (more connections = slightly larger node)"
    - "Edge rendering uses curved lines with arrowheads for directed relationships"
    - "Edge labels have a semi-transparent backdrop for readability"
    - "GraphLegend component shows all unique labels with their assigned colors as colored circles"
    - "GraphLegend renders as an overlay in the bottom-left corner of the graph canvas"
    - "Dark mode colors use deeper backgrounds (#0f0f1a) with vibrant node colors for contrast"
    - "Light mode colors are clean with subtle borders and softer node colors"
    - "Canvas background uses a subtle dot grid pattern for depth"
    - "Tailwind config extends with animation keyframes: fadeIn, slideUp, slideIn for landing page sections"
    - "CSS custom properties define a refined color palette that works in both light and dark themes"
    - "tailwindcss-animate plugin is configured for shadcn/ui animation support"
  artifacts:
    - path: "frontend/src/components/graph/NodeRenderer.ts"
      provides: "Polished canvas node painting with gradients, glow, scaled radius, better labels"
      exports: ["paintNode", "getLabelColor", "LABEL_COLORS"]
    - path: "frontend/src/components/graph/useGraphColors.ts"
      provides: "Refined theme-aware color palette for graph canvas"
      exports: ["useGraphColors", "CanvasColors"]
    - path: "frontend/src/components/graph/GraphLegend.tsx"
      provides: "Overlay legend showing node labels and their colors"
      exports: ["GraphLegend"]
    - path: "frontend/src/components/graph/GraphCanvas.tsx"
      provides: "Enhanced graph canvas with legend overlay and refined force config"
      exports: ["GraphCanvas"]
  key_links:
    - from: "frontend/src/components/graph/GraphCanvas.tsx"
      to: "frontend/src/components/graph/GraphLegend.tsx"
      via: "Renders GraphLegend as overlay inside canvas container"
      pattern: "GraphLegend"
    - from: "frontend/src/components/graph/GraphCanvas.tsx"
      to: "frontend/src/components/graph/NodeRenderer.ts"
      via: "Uses paintNode for canvas node rendering"
      pattern: "paintNode"
---

<objective>
Polish the graph visualization to production quality and establish the design system foundation.

Purpose: Upgrade node rendering with gradients, glow effects, and better typography. Add a graph legend component. Refine the color palette for both themes. Extend Tailwind config with animation utilities needed by landing page and playground plans.

Output: Visually polished graph rendering that looks mature and professional, not like a prototype. Graph legend overlay. Animation CSS utilities ready for downstream plans.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-polish-and-showcase/05-CONTEXT.md

<interfaces>
From frontend/src/components/graph/NodeRenderer.ts (CURRENT — will be heavily modified):
```typescript
export const LABEL_COLORS = [
  '#6366f1', '#ec4899', '#f59e0b', '#10b981', '#3b82f6', '#8b5cf6',
  '#ef4444', '#14b8a6', '#f97316', '#06b6d4', '#84cc16', '#a855f7',
]

export function paintNode(
  node: GraphNode, ctx: CanvasRenderingContext2D, globalScale: number,
  colors: CanvasColors, labelIndex: Map<string, number>
) {
  // Currently: flat fill circle, basic label text
}
```

From frontend/src/components/graph/useGraphColors.ts (CURRENT — will be refined):
```typescript
export interface CanvasColors {
  bg: string; text: string; edge: string; border: string; nodeText: string;
}
// Dark: bg=#1a1a2e, Light: bg=#ffffff — basic palette
```

From frontend/src/components/graph/GraphCanvas.tsx:
```typescript
export function GraphCanvas({ graphData }: GraphCanvasProps) {
  // Uses ForceGraph2D with nodeCanvasObject, linkCanvasObject
  // Container div with ref for resize observer
}
```

From frontend/src/types/graph.ts:
```typescript
export interface GraphNode {
  id: string | number; labels: string[]; properties: Record<string, unknown>;
  label?: string; x?: number; y?: number;
}
export interface GraphEdge {
  id: string | number; source: string | number | GraphNode;
  target: string | number | GraphNode; type: string; properties: Record<string, unknown>;
}
export interface GraphData { nodes: GraphNode[]; links: GraphEdge[]; }
```

From frontend/tailwind.config.js: Standard Tailwind v3 config with shadcn/ui theme extensions. tailwindcss-animate is already a devDependency.

From frontend/src/index.css: Standard Tailwind directives with shadcn/ui CSS custom properties.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Tailwind config and CSS with animation utilities and refined palette</name>
  <files>frontend/tailwind.config.js, frontend/src/index.css</files>
  <action>
1. Read `frontend/tailwind.config.js` and extend the `theme.extend` section:
   - Add `keyframes`:
     ```
     fadeIn: { '0%': { opacity: '0' }, '100%': { opacity: '1' } }
     slideUp: { '0%': { opacity: '0', transform: 'translateY(20px)' }, '100%': { opacity: '1', transform: 'translateY(0)' } }
     slideIn: { '0%': { opacity: '0', transform: 'translateX(-20px)' }, '100%': { opacity: '1', transform: 'translateX(0)' } }
     scaleIn: { '0%': { opacity: '0', transform: 'scale(0.95)' }, '100%': { opacity: '1', transform: 'scale(1)' } }
     ```
   - Add `animation`:
     ```
     'fade-in': 'fadeIn 0.5s ease-out'
     'slide-up': 'slideUp 0.5s ease-out'
     'slide-in': 'slideIn 0.5s ease-out'
     'scale-in': 'scaleIn 0.3s ease-out'
     ```
   - Ensure `plugins` array includes `require('tailwindcss-animate')`

2. Read `frontend/src/index.css` and add utility classes after the Tailwind directives:
   - Add `.animate-delay-100 { animation-delay: 100ms; }` through `.animate-delay-500` (100ms increments)
   - Add `.animate-fill-both { animation-fill-mode: both; }` for proper fade-in staggering
   - Add `.glass { @apply bg-background/80 backdrop-blur-md; }` utility for glass effect
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>Tailwind config has fadeIn, slideUp, slideIn, scaleIn animations. CSS has delay utilities and glass effect class.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade useGraphColors with refined dark/light palette</name>
  <files>frontend/src/components/graph/useGraphColors.ts</files>
  <action>
1. Read `frontend/src/components/graph/useGraphColors.ts`.

2. Extend the `CanvasColors` interface with new fields:
   ```typescript
   export interface CanvasColors {
     bg: string
     text: string
     edge: string
     border: string
     nodeText: string
     gridDot: string      // dot grid pattern color
     nodeShadow: string   // glow shadow color base
     edgeLabel: string     // edge label text color
     edgeLabelBg: string   // edge label background
   }
   ```

3. Update the dark theme colors for a deeper, more premium feel:
   ```typescript
   // Dark theme
   {
     bg: '#0f0f1a',           // deeper dark blue-black
     text: '#e2e8f0',          // slightly warmer white
     edge: '#334155',          // slate-700 — subtle but visible
     border: '#1e293b',        // slate-800
     nodeText: '#f1f5f9',      // slate-100 — bright for contrast
     gridDot: '#1e293b',       // subtle dot grid
     nodeShadow: 'rgba(99, 102, 241, 0.3)', // indigo glow base
     edgeLabel: '#94a3b8',     // slate-400
     edgeLabelBg: 'rgba(15, 15, 26, 0.85)', // bg with alpha
   }
   ```

4. Update light theme colors for clean, refined look:
   ```typescript
   // Light theme
   {
     bg: '#fafbfc',            // slightly warm white
     text: '#0f172a',          // slate-900
     edge: '#cbd5e1',          // slate-300
     border: '#e2e8f0',        // slate-200
     nodeText: '#1e293b',      // slate-800
     gridDot: '#e2e8f0',       // subtle dot grid
     nodeShadow: 'rgba(99, 102, 241, 0.15)', // softer glow
     edgeLabel: '#64748b',     // slate-500
     edgeLabelBg: 'rgba(250, 251, 252, 0.85)', // bg with alpha
   }
   ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>CanvasColors interface extended with gridDot, nodeShadow, edgeLabel, edgeLabelBg. Both themes use refined slate-based palette.</done>
</task>

<task type="auto">
  <name>Task 3: Polish NodeRenderer with gradients, glow, and better typography</name>
  <files>frontend/src/components/graph/NodeRenderer.ts</files>
  <action>
1. Read `frontend/src/components/graph/NodeRenderer.ts`.

2. Update `LABEL_COLORS` to a curated palette that looks vibrant on both dark and light backgrounds:
   ```typescript
   export const LABEL_COLORS = [
     '#818cf8', // indigo-400
     '#f472b6', // pink-400
     '#fbbf24', // amber-400
     '#34d399', // emerald-400
     '#60a5fa', // blue-400
     '#a78bfa', // violet-400
     '#fb923c', // orange-400
     '#2dd4bf', // teal-400
     '#e879f9', // fuchsia-400
     '#38bdf8', // sky-400
     '#a3e635', // lime-400
     '#fb7185', // rose-400
   ]
   ```

3. Rewrite `paintNode` with polished rendering:
   ```typescript
   export function paintNode(
     node: GraphNode,
     ctx: CanvasRenderingContext2D,
     globalScale: number,
     colors: CanvasColors,
     labelIndex: Map<string, number>,
     connectionCounts?: Map<string | number, number>
   ) {
     const label = node.label || node.labels?.[0] || String(node.id)
     const displayName = node.properties?.name || node.properties?.title || label
     const nodeColor = getLabelColor(node.labels?.[0] || 'default', labelIndex)

     // Scale radius by connection count (min 5, max 12)
     const connections = connectionCounts?.get(node.id) ?? 0
     const baseRadius = 5 + Math.min(connections * 0.5, 7)
     const radius = baseRadius
     const fontSize = Math.max(11 / globalScale, 2.5)

     const x = node.x!
     const y = node.y!

     // Outer glow
     ctx.save()
     ctx.shadowColor = nodeColor
     ctx.shadowBlur = 8
     ctx.beginPath()
     ctx.arc(x, y, radius, 0, 2 * Math.PI)
     ctx.fillStyle = 'transparent'
     ctx.fill()
     ctx.restore()

     // Radial gradient fill (lighter center, darker edge)
     const gradient = ctx.createRadialGradient(
       x - radius * 0.3, y - radius * 0.3, radius * 0.1,
       x, y, radius
     )
     gradient.addColorStop(0, lightenColor(nodeColor, 30))
     gradient.addColorStop(0.7, nodeColor)
     gradient.addColorStop(1, darkenColor(nodeColor, 20))

     ctx.beginPath()
     ctx.arc(x, y, radius, 0, 2 * Math.PI)
     ctx.fillStyle = gradient
     ctx.fill()

     // Subtle inner border
     ctx.strokeStyle = darkenColor(nodeColor, 30)
     ctx.lineWidth = 0.5
     ctx.stroke()

     // Label text with shadow for readability
     ctx.font = `500 ${fontSize}px Inter, system-ui, -apple-system, sans-serif`
     ctx.textAlign = 'center'
     ctx.textBaseline = 'top'

     const truncated = String(displayName).length > 18
       ? String(displayName).slice(0, 15) + '...'
       : String(displayName)

     // Text shadow for readability
     ctx.shadowColor = colors.bg
     ctx.shadowBlur = 3
     ctx.fillStyle = colors.nodeText
     ctx.fillText(truncated, x, y + radius + 3)
     ctx.shadowBlur = 0
   }
   ```

4. Add color utility functions at the top of the file:
   ```typescript
   function lightenColor(hex: string, percent: number): string {
     const num = parseInt(hex.replace('#', ''), 16)
     const r = Math.min(255, (num >> 16) + Math.round(2.55 * percent))
     const g = Math.min(255, ((num >> 8) & 0x00ff) + Math.round(2.55 * percent))
     const b = Math.min(255, (num & 0x0000ff) + Math.round(2.55 * percent))
     return `#${(r << 16 | g << 8 | b).toString(16).padStart(6, '0')}`
   }

   function darkenColor(hex: string, percent: number): string {
     const num = parseInt(hex.replace('#', ''), 16)
     const r = Math.max(0, (num >> 16) - Math.round(2.55 * percent))
     const g = Math.max(0, ((num >> 8) & 0x00ff) - Math.round(2.55 * percent))
     const b = Math.max(0, (num & 0x0000ff) - Math.round(2.55 * percent))
     return `#${(r << 16 | g << 8 | b).toString(16).padStart(6, '0')}`
   }
   ```

5. Export `connectionCounts` as an optional parameter so GraphCanvas can compute and pass it.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>paintNode uses radial gradients, outer glow, scaled radius by connection count, Inter font with text shadow. Color utilities for lighten/darken. Display name shows property title/name instead of just label.</done>
</task>

<task type="auto">
  <name>Task 4: Create GraphLegend overlay component</name>
  <files>frontend/src/components/graph/GraphLegend.tsx</files>
  <action>
1. Create `frontend/src/components/graph/GraphLegend.tsx`:
   ```typescript
   import { LABEL_COLORS, getLabelColor } from './NodeRenderer'

   interface GraphLegendProps {
     labels: string[]
     labelIndex: Map<string, number>
   }

   export function GraphLegend({ labels, labelIndex }: GraphLegendProps) {
     if (labels.length === 0) return null

     return (
       <div className="absolute bottom-3 left-3 rounded-lg border bg-background/80 backdrop-blur-sm px-3 py-2 shadow-sm">
         <p className="mb-1.5 text-[10px] font-medium uppercase tracking-wider text-muted-foreground">
           Legend
         </p>
         <div className="flex flex-col gap-1">
           {labels.map((label) => (
             <div key={label} className="flex items-center gap-2">
               <span
                 className="inline-block h-2.5 w-2.5 rounded-full"
                 style={{ backgroundColor: getLabelColor(label, labelIndex) }}
               />
               <span className="text-xs text-foreground">{label}</span>
             </div>
           ))}
         </div>
       </div>
     )
   }
   ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>GraphLegend component renders as a glass-effect overlay with colored circles per unique label.</done>
</task>

<task type="auto">
  <name>Task 5: Enhance GraphCanvas with legend, dot grid, refined force config, and connection counts</name>
  <files>frontend/src/components/graph/GraphCanvas.tsx</files>
  <action>
1. Read `frontend/src/components/graph/GraphCanvas.tsx`.

2. Add imports for GraphLegend:
   ```typescript
   import { GraphLegend } from './GraphLegend'
   ```

3. Compute connection counts from graphData for scaled node sizes:
   ```typescript
   const connectionCounts = useMemo(() => {
     const counts = new Map<string | number, number>()
     for (const link of graphData.links) {
       const sourceId = typeof link.source === 'object' ? (link.source as GraphNode).id : link.source
       const targetId = typeof link.target === 'object' ? (link.target as GraphNode).id : link.target
       counts.set(sourceId, (counts.get(sourceId) ?? 0) + 1)
       counts.set(targetId, (counts.get(targetId) ?? 0) + 1)
     }
     return counts
   }, [graphData])
   ```

4. Compute unique labels for legend:
   ```typescript
   const uniqueLabels = useMemo(() => {
     const labels = new Set<string>()
     for (const node of graphData.nodes) {
       if (node.labels?.[0]) labels.add(node.labels[0])
     }
     return Array.from(labels).sort()
   }, [graphData])
   ```

5. Update `nodeCanvasObject` to pass connectionCounts to paintNode:
   ```typescript
   const nodeCanvasObject = useCallback(
     (node: GraphNode, ctx: CanvasRenderingContext2D, globalScale: number) => {
       paintNode(node, ctx, globalScale, colors, labelIndex, connectionCounts)
     },
     [colors, labelIndex, connectionCounts]
   )
   ```

6. Update `linkCanvasObject` to use new color fields (edgeLabel, edgeLabelBg):
   - Replace `colors.bg` with `colors.edgeLabelBg` for backdrop
   - Replace `colors.text` with `colors.edgeLabel` for text

7. Add a custom canvas background painting function that draws a subtle dot grid:
   ```typescript
   const onRenderFramePre = useCallback(
     (ctx: CanvasRenderingContext2D, globalScale: number) => {
       // Only draw grid when zoomed in enough for dots to be visible
       if (globalScale < 0.5) return
       const dotSpacing = 30
       const dotRadius = 0.5
       ctx.fillStyle = colors.gridDot
       // ... draw dots across visible area
     },
     [colors.gridDot]
   )
   ```
   NOTE: react-force-graph-2d does not have an `onRenderFramePre` prop. Instead, draw the dot grid INSIDE the container div using a CSS background pattern:
   ```
   backgroundImage: `radial-gradient(circle, ${colors.gridDot} 1px, transparent 1px)`,
   backgroundSize: '30px 30px',
   ```

8. Update ForceGraph2D config for smoother physics:
   - `d3AlphaDecay={0.015}` (slower settling for smoother animation)
   - `d3VelocityDecay={0.25}` (less damping for fluid feel)
   - `cooldownTicks={150}` (more ticks before freeze)
   - `linkDistance={60}` (more spacing between nodes)
   - `nodeRelSize={1}` (use our custom sizing)

9. Wrap the container div to include the legend overlay:
   ```tsx
   return (
     <div ref={containerRef} className="relative w-full h-full">
       <div
         className="absolute inset-0"
         style={{
           backgroundColor: colors.bg,
           backgroundImage: `radial-gradient(circle, ${colors.gridDot} 1px, transparent 1px)`,
           backgroundSize: '30px 30px',
         }}
       />
       <ForceGraph2D ... />
       <GraphLegend labels={uniqueLabels} labelIndex={labelIndex} />
     </div>
   )
   ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>GraphCanvas renders with dot grid background, legend overlay, connection-scaled nodes, refined force physics. Edge labels use new edgeLabel/edgeLabelBg colors.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript compiles without errors
2. `cd frontend && npm run build` — Build succeeds
3. `cd frontend && npm run test:unit` — Existing tests still pass
4. Visual: Open /playground — graph nodes show gradient fills with glow, scaled by connections
5. Visual: Legend overlay shows in bottom-left with colored circles per label
6. Visual: Toggle dark mode — colors are deeper and more vibrant, dot grid visible
7. Visual: Edge labels have backdrop and readable text
</verification>

<success_criteria>
- Nodes render with radial gradient, outer glow, and connection-based sizing
- Labels show node name/title (not just type label), truncated if needed
- GraphLegend overlay shows all unique labels with color mapping
- Dot grid background visible in graph canvas
- Dark mode uses deep #0f0f1a background with vibrant node colors
- Light mode uses clean #fafbfc with softer palette
- Tailwind animation utilities (fadeIn, slideUp, slideIn, scaleIn) available
- All existing tests pass, TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-polish-and-showcase/05-01-SUMMARY.md`
</output>
