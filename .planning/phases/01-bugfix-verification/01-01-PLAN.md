---
phase: 01-bugfix-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ogdb-core/src/lib.rs
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-04
  - BUG-05
  - BUG-06
  - BUG-07
  - BUG-08
  - BUG-09

must_haves:
  truths:
    - "MATCH (p:Person {name: 'Alice'}) returns only Alice, not all Person nodes"
    - "RETURN a.name, c.name produces columns named name and name_2 without error"
    - "CREATE INDEX ON :Label(property) completes and the index appears in db.list_indexes()"
    - "CREATE INDEX FOR (n:Label) ON (n.prop) completes and the index appears in db.list_indexes()"
    - "CALL db.indexes() and CALL db.algo.shortestPath(src, dst) dispatch correctly"
    - "CALL db.index.fulltext.queryNodes supports 1-3 argument forms with property-scan fallback"
    - "Relationship property projection returns null for missing properties instead of error"
    - "ORDER BY on numeric values sorts by numeric value, not string/insertion order"
    - "REMOVE n.prop executes without error and the property reads as null afterward"
  artifacts:
    - path: "crates/ogdb-core/src/lib.rs"
      provides: "All 9 query engine bugfix regression tests"
      contains: "cypher_match_applies_inline_node_property_filters"
  key_links:
    - from: "crates/ogdb-core/src/lib.rs (tests)"
      to: "Database::query()"
      via: "db.query(...) API call in each test"
      pattern: 'db\.query\('
---

<objective>
Audit and verify all 9 query engine / parser bugfix regression tests in ogdb-core (BUG-01 through BUG-09).

Purpose: Research confirmed that regression tests exist for all 9 bugs. This plan audits each test to confirm it exercises the specific bugfix behavior described in REQUIREMENTS.md, and strengthens or adds tests only where gaps are found. This is verification work, not re-implementation.

Output: All 9 ogdb-core regression tests confirmed adequate; any gaps filled with targeted additional assertions or tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bugfix-verification/01-RESEARCH.md

@crates/ogdb-core/src/lib.rs (test module at bottom of file, starting around line 20000+)

<interfaces>
Test helpers available in ogdb-core test module:
```rust
fn temp_db_path(tag: &str) -> PathBuf;
fn cleanup_db_artifacts(path: &Path);
```

Database API used by all tests:
```rust
Database::init(path: &Path, header: Header) -> Result<Database, DbError>
Database::query(cypher: &str) -> Result<QueryResult, QueryError>
QueryResult::to_rows() -> Vec<Vec<PropertyValue>>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit regression tests for BUG-01 through BUG-05</name>
  <files>crates/ogdb-core/src/lib.rs</files>
  <action>
For each of the 5 bugs below, locate the existing test function in the `#[cfg(test)] mod tests` block at the bottom of `crates/ogdb-core/src/lib.rs`, read its body, and verify it covers the requirement. Record your findings. Only modify the file if a gap is found.

**BUG-01: MATCH inline node-property filters**
- Find: `cypher_match_applies_inline_node_property_filters` (~line 25827)
- Also find: `cypher_match_with_two_patterns_filters_before_create` (~line 25866)
- Verify: First test creates multiple Person nodes, queries with `{name: 'Alice'}` filter, asserts only Alice is returned (not all Person nodes). Second test covers multi-pattern MATCH with inline properties.
- Requirement met when: Test asserts `rows.len() == 1` and the returned name is "Alice".

**BUG-02: Duplicate column disambiguation**
- Find: `cypher_query_disambiguates_duplicate_projection_output_names` (~line 25781)
- Verify: Test creates nodes and queries with `RETURN a.name, c.name`, asserts column names include `name` and `name_2`, and row values are correct.
- Requirement met when: Test asserts disambiguated column names and correct values.

**BUG-03: CREATE INDEX ON :Label(property)**
- Find: `cypher_query_executes_create_index_on_statement` (~line 25706)
- Verify: Test runs `CREATE INDEX ON :Label(property)` via `db.query(...)` and asserts the index appears in `db.list_indexes()` (or equivalent).
- Requirement met when: Test asserts index exists after creation.

**BUG-04: CALL db.indexes() and db.algo.shortestPath dispatch**
- Find: `builtin_shortest_path_and_indexes_calls_return_expected_rows` (~line 30739)
- Verify: Test runs `CALL db.indexes()` and `CALL db.algo.shortestPath(src, dst)` and asserts both return non-error results with expected row structure.
- Requirement met when: Both CALL forms execute successfully and return rows.

**BUG-05: fulltext.queryNodes 1-3 arg forms**
- Find: `fulltext_builtin_call_without_indexes_falls_back_to_property_scan` (~line 29758)
- Also find: `builtin_call_procedure_validation_paths` (~line 30039)
- Verify: Tests exercise 1-arg (query text only), 2-arg (index name + text), and 3-arg (index name + text + k) forms. The fallback path returns results via property scan when no fulltext index exists.
- Requirement met when: All three argument forms are tested and produce non-error results.

**If a gap is found in any test:** Add minimal assertions or a new test following the existing pattern (temp_db_path, seed data, db.query, assert, cleanup_db_artifacts). Do NOT add the `#[cfg(feature = "fulltext-search")]` gate to any BUG-05 fallback tests (the fallback path runs without the feature).

**If all tests are adequate:** Do not modify the file. Document in your summary that all 5 tests were audited and found sufficient.
  </action>
  <verify>
    <automated>cd /Users/ashesh/opengraphdb && cargo test -p ogdb-core -- cypher_match_applies_inline_node_property_filters cypher_match_with_two_patterns_filters_before_create cypher_query_disambiguates_duplicate_projection_output_names cypher_query_executes_create_index_on_statement builtin_shortest_path_and_indexes_calls_return_expected_rows fulltext_builtin_call_without_indexes_falls_back_to_property_scan builtin_call_procedure_validation_paths 2>&1 | tail -20</automated>
  </verify>
  <done>All 7 test functions for BUG-01 through BUG-05 pass. Each test exercises the specific bugfix behavior described in REQUIREMENTS.md. Any coverage gaps have been filled.</done>
</task>

<task type="auto">
  <name>Task 2: Audit regression tests for BUG-06 through BUG-09</name>
  <files>crates/ogdb-core/src/lib.rs</files>
  <action>
For each of the 4 bugs below, locate the existing test function, read its body, and verify it covers the requirement. Only modify the file if a gap is found.

**BUG-06: Relationship property projection null for missing props**
- Find: `relationship_property_projection_allows_missing_values_as_null` (~line 30530)
- Verify: Test creates edges with and without a property (e.g., `since`), projects the property, and asserts missing values return null (PropertyValue string "null") instead of an inconsistent-type error.
- Requirement met when: Test has edges with mixed property presence and asserts null for the missing one.

**BUG-07: Numeric ORDER BY sorts by value**
- Find: `cypher_query_orders_integers_by_numeric_value_not_lexical_or_insertion_order` (~line 25742)
- Verify: Test inserts nodes with numeric properties in non-sorted insertion order (e.g., ages 10, 2, 1), runs `ORDER BY n.age ASC`, and asserts the result is `[1, 2, 10]` (numeric sort, not lexical "1", "10", "2").
- Requirement met when: Test asserts the correct numeric ordering.

**BUG-08: REMOVE n.prop**
- Find: `cypher_query_executes_remove_property_and_returns_null_property` (~line 25677)
- Verify: Test creates a node with a property, runs `REMOVE n.prop`, and asserts the property reads as null afterward.
- Requirement met when: Test asserts post-REMOVE property is null.

**BUG-09: CREATE INDEX FOR (n:Label) ON (n.prop)**
- Find: `cypher_query_executes_create_index_for_on_statement` (~line 25724)
- Verify: Test runs the `CREATE INDEX FOR (n:Label) ON (n.prop)` alternate syntax and asserts the index is created successfully (mirrors BUG-03 test with different syntax).
- Requirement met when: Test asserts index exists after creation with the FOR/ON syntax.

**If a gap is found:** Add minimal assertions or a new test following the existing codebase pattern. If all tests are adequate, do not modify the file.
  </action>
  <verify>
    <automated>cd /Users/ashesh/opengraphdb && cargo test -p ogdb-core -- relationship_property_projection_allows_missing_values_as_null cypher_query_orders_integers_by_numeric_value_not_lexical_or_insertion_order cypher_query_executes_remove_property_and_returns_null_property cypher_query_executes_create_index_for_on_statement 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 test functions for BUG-06 through BUG-09 pass. Each test exercises the specific bugfix behavior described in REQUIREMENTS.md. Any coverage gaps have been filled.</done>
</task>

</tasks>

<verification>
Run all ogdb-core tests to confirm no regressions:
```bash
cargo test -p ogdb-core 2>&1 | tail -5
```
Expected: all tests pass (308+ tests, 0 failures).
</verification>

<success_criteria>
- All 11 test functions covering BUG-01 through BUG-09 have been audited
- Each test exercises the specific behavior described in REQUIREMENTS.md
- All tests pass when run with `cargo test -p ogdb-core`
- Any gaps found have been filled with targeted additions following existing test patterns
</success_criteria>

<output>
After completion, create `.planning/phases/01-bugfix-verification/01-01-SUMMARY.md`
</output>
