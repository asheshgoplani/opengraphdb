---
phase: 04-landing-page-and-playground
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/pages/LandingPage.tsx
  - frontend/src/components/landing/HeroSection.tsx
  - frontend/src/components/landing/FeaturesSection.tsx
  - frontend/src/components/landing/GettingStartedSection.tsx
  - frontend/src/components/ui/card.tsx
autonomous: true
requirements: [DEMO-01, DEMO-02]

must_haves:
  truths:
    - "User lands on / and sees a hero section with OpenGraphDB headline, value proposition subtext, and two CTA buttons (Try Playground, Open App)"
    - "User sees a features section with 3-4 cards highlighting OpenGraphDB differentiators (Rust core, Cypher-first, AI/MCP access, embeddable)"
    - "User sees a getting started section with numbered steps explaining how to install and use OpenGraphDB"
    - "Landing page respects dark mode via existing ThemeProvider (uses Tailwind semantic tokens, no hardcoded colors)"
    - "Try Playground button navigates to /playground and Open App button navigates to /app using React Router Link"
    - "Navigation bar at top has OpenGraphDB wordmark and links to Playground and Open App"
  artifacts:
    - path: "frontend/src/pages/LandingPage.tsx"
      provides: "Landing page route component composing three sections"
      exports: ["default"]
    - path: "frontend/src/components/landing/HeroSection.tsx"
      provides: "Hero section with headline, subtext, and CTA buttons"
      exports: ["HeroSection"]
    - path: "frontend/src/components/landing/FeaturesSection.tsx"
      provides: "Feature cards grid showing 3-4 differentiators"
      exports: ["FeaturesSection"]
    - path: "frontend/src/components/landing/GettingStartedSection.tsx"
      provides: "Numbered steps quick-start guide"
      exports: ["GettingStartedSection"]
    - path: "frontend/src/components/ui/card.tsx"
      provides: "shadcn Card component for feature cards"
      exports: ["Card", "CardHeader", "CardTitle", "CardDescription", "CardContent"]
  key_links:
    - from: "frontend/src/pages/LandingPage.tsx"
      to: "frontend/src/components/landing/HeroSection.tsx"
      via: "Component import and render"
      pattern: "import.*HeroSection"
    - from: "frontend/src/components/landing/HeroSection.tsx"
      to: "/playground"
      via: "React Router Link for CTA button"
      pattern: "Link.*to.*playground"
    - from: "frontend/src/components/landing/FeaturesSection.tsx"
      to: "frontend/src/components/ui/card.tsx"
      via: "shadcn Card components for feature cards"
      pattern: "import.*Card.*ui/card"
---

<objective>
Build the landing page with hero section, feature highlights, and getting started guide that explains OpenGraphDB's key differentiators to first-time evaluators.

Purpose: Give evaluators an immediate understanding of what OpenGraphDB is and why it matters, with clear navigation to the playground and main app.

Output: LandingPage.tsx as a default export (lazy-loaded by AppRouter from 04-01), three section components, and the shadcn Card component.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-landing-page-and-playground/04-RESEARCH.md
@.planning/phases/04-landing-page-and-playground/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/AppRouter.tsx (created in 04-01):
```typescript
const LandingPage = lazy(() => import('./pages/LandingPage'))
// LandingPage MUST be a default export from frontend/src/pages/LandingPage.tsx
```

From frontend/src/components/ui/button.tsx (existing):
```typescript
// shadcn Button with variants: default, destructive, outline, secondary, ghost, link
// Supports asChild prop for Link composition
import { Button } from '@/components/ui/button'
```

Existing UI components available: Button, Badge, Input, Table, Dialog, Sheet
Tailwind v3 with semantic tokens: bg-background, text-foreground, text-muted-foreground, bg-card, bg-muted, border

OpenGraphDB differentiators (from ARCHITECTURE.md):
1. Rust-native graph engine: no JVM/GC dependency, single binary, embeddable
2. Cypher-first query language with openCypher compatibility
3. AI-friendly: MCP server + structured machine-readable exports
4. Graph-native storage: double CSR edge layout, not graph-over-relational
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Card and create landing page section components</name>
  <files>frontend/src/components/ui/card.tsx, frontend/src/components/landing/HeroSection.tsx, frontend/src/components/landing/FeaturesSection.tsx, frontend/src/components/landing/GettingStartedSection.tsx</files>
  <action>
1. Install the shadcn Card component:
   ```bash
   cd frontend && npx shadcn@latest add card --yes
   ```
   Verify `frontend/src/components/ui/card.tsx` was created. If the CLI fails, manually create it from the shadcn Card source (it is a simple styled div wrapper).

2. Create `frontend/src/components/landing/HeroSection.tsx`:
   - Import `Link` from `react-router-dom`, `Button` from `@/components/ui/button`
   - Export named `HeroSection` component
   - Structure:
     - Outer `<section className="py-20 sm:py-32 px-4">` with `<div className="max-w-4xl mx-auto text-center">`
     - Headline: `<h1 className="text-4xl sm:text-6xl font-bold tracking-tight">` with text "The Graph Database Built for Speed"
     - Subtext: `<p className="mt-6 text-lg sm:text-xl text-muted-foreground max-w-2xl mx-auto">` with text: "OpenGraphDB is a Rust-native, embeddable graph database with Cypher query support, blazing-fast traversals, and first-class AI integration through MCP."
     - CTA buttons row: `<div className="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4">`
       - Primary: `<Button size="lg" asChild><Link to="/playground">Try the Playground</Link></Button>`
       - Secondary: `<Button size="lg" variant="outline" asChild><Link to="/app">Open App</Link></Button>`

3. Create `frontend/src/components/landing/FeaturesSection.tsx`:
   - Import `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent` from `@/components/ui/card`
   - Import icons from `lucide-react`: `Zap` (speed), `Terminal` (Cypher), `Bot` (AI), `Database` (storage)
   - Export named `FeaturesSection` component
   - Structure:
     - Outer `<section className="py-16 px-4 bg-muted/50">` with `<div className="max-w-5xl mx-auto">`
     - Section title: `<h2 className="text-2xl sm:text-3xl font-bold text-center mb-12">` with text "Why OpenGraphDB?"
     - Grid: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">`
     - Four Card items, each with icon, title, and description:
       1. Zap icon: "Blazing Fast" / "Rust-native engine with double CSR edge layout. No JVM, no garbage collector. Sub-millisecond traversals."
       2. Terminal icon: "Cypher-First" / "Full openCypher query language with syntax highlighting, autocomplete, and query history built in."
       3. Bot icon: "AI-Ready" / "First-class MCP server integration. Let AI agents query and explore your graph data natively."
       4. Database icon: "Embeddable" / "Single binary, zero external dependencies. Embed directly in your application or run standalone."

4. Create `frontend/src/components/landing/GettingStartedSection.tsx`:
   - Export named `GettingStartedSection` component
   - Structure:
     - Outer `<section className="py-16 px-4">` with `<div className="max-w-3xl mx-auto">`
     - Section title: `<h2 className="text-2xl sm:text-3xl font-bold text-center mb-12">` with text "Get Started in Minutes"
     - Three numbered steps, each as a `<div>` with step number, title, and code/description:
       1. "Install OpenGraphDB" with code block: `cargo install opengraphdb`
       2. "Start the Server" with code block: `opengraphdb serve --port 8080`
       3. "Query Your Graph" with code block: `MATCH (n) RETURN n LIMIT 25`
     - Each step: `<div className="flex gap-4 mb-8">` with a number badge `<div className="flex-shrink-0 w-8 h-8 rounded-full bg-primary text-primary-foreground flex items-center justify-center font-bold text-sm">` and content div
     - Code blocks: `<code className="block mt-2 bg-muted px-3 py-2 rounded-md text-sm font-mono">`

All components use ONLY Tailwind semantic tokens (bg-background, text-foreground, text-muted-foreground, bg-muted, bg-card, bg-primary, text-primary-foreground, border). No hardcoded hex colors.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Card component exists. HeroSection, FeaturesSection, and GettingStartedSection compile without errors and use only Tailwind semantic tokens for dark mode compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Create LandingPage route component with navigation bar</name>
  <files>frontend/src/pages/LandingPage.tsx</files>
  <action>
1. Create `frontend/src/pages/LandingPage.tsx`:
   - Import `Link` from `react-router-dom`
   - Import `Button` from `@/components/ui/button`
   - Import `HeroSection` from `@/components/landing/HeroSection`
   - Import `FeaturesSection` from `@/components/landing/FeaturesSection`
   - Import `GettingStartedSection` from `@/components/landing/GettingStartedSection`
   - Export DEFAULT function `LandingPage` (MUST be default export for React.lazy in AppRouter)
   - Structure:
     ```tsx
     <div className="min-h-screen bg-background text-foreground">
       <nav className="sticky top-0 z-50 h-12 border-b bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60 flex items-center px-4 justify-between">
         <span className="font-semibold">OpenGraphDB</span>
         <div className="flex gap-2">
           <Button variant="ghost" size="sm" asChild>
             <Link to="/playground">Playground</Link>
           </Button>
           <Button size="sm" asChild>
             <Link to="/app">Open App</Link>
           </Button>
         </div>
       </nav>
       <HeroSection />
       <FeaturesSection />
       <GettingStartedSection />
       <footer className="py-8 px-4 border-t text-center text-sm text-muted-foreground">
         OpenGraphDB — Open-source graph database
       </footer>
     </div>
     ```

2. Verify the page renders at `/` by running the dev server and checking for TypeScript compilation.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>LandingPage.tsx is a default export that composes nav bar, hero section, features section, getting started section, and footer. All imports resolve. Dark mode works via semantic tokens. Navigation links point to /playground and /app.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript compiles without errors
2. `cd frontend && npm run build` — Build succeeds; landing page chunk is code-split via React.lazy
3. Manual: Navigate to `/` — landing page shows hero, features cards, and getting started steps
4. Manual: Click "Try the Playground" — navigates to /playground
5. Manual: Click "Open App" — navigates to /app and shows query interface
6. Manual: Toggle dark mode from /app then navigate back to / — landing page respects theme
</verification>

<success_criteria>
- LandingPage renders at / with hero section, feature cards, and getting started guide
- Hero section has headline, value proposition, and two working CTA buttons
- Features section shows 4 cards with OpenGraphDB differentiators using shadcn Card
- Getting started section has 3 numbered steps with code examples
- All components use Tailwind semantic tokens (dark mode works)
- Navigation between landing page and /app works via React Router Link
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-landing-page-and-playground/04-02-SUMMARY.md`
</output>
